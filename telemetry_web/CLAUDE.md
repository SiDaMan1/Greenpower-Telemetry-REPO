# CLAUDE.md — Telemetry Web

**This file is a briefing for an AI assistant.** Read it before touching any code in this folder.

---

## What This Project Is

The always-on public dashboard for Greenpower telemetry. A small Node/Express app meant to run on Railway 24/7, reachable at whatever domain is pointed at it (see the GoDaddy DNS notes below). It does **not** talk to the ESP32 receiver directly — [`receiver_agent`](../receiver_agent/CLAUDE.md) does that, and POSTs live packets here whenever it's actively forwarding. Live data still flows through an in-memory value the same as before; as of this pass, every packet is **also** persisted to Postgres and grouped into automatically-bounded "sessions" so past drives/tests can be browsed and exported later, not just watched live.

The three-part system: `greenpower_receiver` (ESP32, LoRa → USB serial) → `receiver_agent` (runs on whatever computer the receiver is plugged into, watches for it, asks the user before forwarding) → `telemetry_web` (this folder, always-on public site).

---

## Files in This Folder

| File | Role | May be edited? |
|------|------|----------------|
| `server.js` | Express app — `/api/telemetry` (POST, authenticated), `/api/latest` (GET, public), session endpoints (GET, public), serves `public/` | **Yes — primary target** |
| `public/index.html` | Dashboard — polls `/api/latest` 5x/sec for live data, fetches session data on demand (not polled), single self-contained file (no build step). Same file serves both desktop and mobile layouts via CSS media queries + a mobile-only sidebar, not a separate page — see PWA/mobile rules below | **Yes** |
| `public/manifest.json` | PWA manifest — name, icons, `display:standalone`, theme colors | **Yes** |
| `public/sw.js` | Minimal service worker — installability only, no caching (see rule below) | **Yes, but keep it caching-free unless offline support is a deliberate feature decision** |
| `public/icon.svg`, `public/icon-maskable.svg` | App icons referenced by `manifest.json` — `icon-maskable.svg` has the bolt shrunk into the safe zone with a full-bleed background | **Yes** |
| `package.json` | Dependencies (`express`, `pg`) | **Yes, if adding a real dependency** |
| `nixpacks.toml` | Overrides Railway's auto-generated install command (`npm ci --omit=dev`) — silences a cosmetic npm deprecation warning in Railway's build logs, see its own rule | **Yes, but rarely needs touching** |
| `CLAUDE.md` | This file | **Yes — update after significant changes** |

---

## Rules and Constraints

### Live status is still in-memory only — persistence is a separate, parallel path
`latest`/`lastUpdateMs` in `server.js` are still plain in-memory variables, cleared on every restart/redeploy, and that's still correct for "is the car live right now." Don't try to derive `/api/latest` from the database — it's deliberately a separate, cheap, always-current path that doesn't depend on Postgres being configured or reachable.

### Graceful shutdown on SIGTERM/SIGINT — this is what stops Railway logging a scary-looking "npm error" on every normal redeploy
Real, reported behavior, not a hypothetical: every Railway redeploy (and manual restart) sends `SIGTERM` to the OLD container to shut it down — this is normal deploy lifecycle, not a crash. Node has no default handler for `SIGTERM`, so without one the process just dies mid-signal, and npm's wrapper reports that as `npm error / command failed / signal SIGTERM` — reading exactly like a real failure in Railway's build/deploy logs even though the app is completely healthy and the new deploy is live. `server.js` now handles both `SIGTERM` and `SIGINT` explicitly: `server.close()` (stop accepting new connections), close the `pg` pool if one's configured, then `process.exit(0)` — npm sees a clean exit code instead of a signal-killed process, which is what actually removes the message, not a config flag anywhere. A 5-second unref'd timeout forces the same clean exit if `server.close()`'s callback never fires for some reason (a stuck connection), so this can't hang past Railway's own grace period before it'd otherwise SIGKILL.
**Only partially verifiable from a Windows dev machine** — `node --check` and a normal local start/stop were verified clean, but Windows doesn't deliver real POSIX signals the way Railway's Linux containers do (`process.kill(pid, 'SIGTERM')` cross-process on Windows does not reliably invoke the target's `process.on('SIGTERM', ...)` handler — confirmed directly: the test process exited without the handler's own log line ever appearing). The code itself is the standard, well-documented Node.js graceful-shutdown pattern and is expected to work correctly on Railway's actual Linux runtime — worth confirming the "npm error" message is actually gone from Railway's logs after the next real deploy, since this environment can't fully prove that end-to-end.

### `nixpacks.toml` pins the install command — silences a cosmetic Railway build warning
Real, reported log noise, not a functional problem: Railway's auto-generated Nixpacks build step sets npm's legacy `production` config internally, which current npm versions flag with `npm warn config production Use --omit=dev instead`. `nixpacks.toml` (`[phases.install] cmds = ["npm ci --omit=dev"]`) overrides Railway's auto-generated install step with the modern equivalent — functionally identical either way here, since `package.json` has no `devDependencies` to omit in the first place. **Not verifiable from this dev environment** — Nixpacks builds only run in Railway's own cloud pipeline, so this is a standard, documented override mechanism applied correctly, not something locally tested end-to-end; confirm the warning is actually gone from Railway's build logs after the next deploy.

### Database is optional — `pool` being `null` must never crash a request
`server.js` only creates the `pg` `Pool` if `DATABASE_URL` is set (Railway injects this automatically once a Postgres plugin is attached — see README.md's setup section). Every DB-touching route (`recordPoint()`, `/api/sessions`, `/api/sessions/:id/points`, `/api/sessions/:id/export.csv`) checks `if (!pool)` first and degrades gracefully (silent skip for recording, `503` for the read endpoints) rather than throwing. Keep that guard on any new DB-touching code — local dev without a configured Postgres, and any deploy where the plugin hasn't been added yet, both depend on this not crashing.

### Telemetry is stored as JSONB, not one column per field — this is deliberate
`telemetry_points.data` is a single `JSONB` column holding the whole packet, not `speed_mph FLOAT, batt_volt FLOAT, ...`. `telemetry_packet_t` has changed shape twice already in this project's history (ESC fields added, RPM fields reworked) — a fixed relational schema would need a migration every time that happens again. JSONB means the DB layer doesn't care what fields exist. The one place a fixed field list *does* exist is `CSV_COLUMNS` in `server.js`, and that's intentional — see below.

### `CSV_COLUMNS` is a fixed, hand-maintained list — keep it in sync with the packet shape
CSV export doesn't derive its columns from whatever keys happen to be in any one row (which would make column order unstable, or silently drop columns missing from row 1). It uses the explicit `CSV_COLUMNS` array in `server.js`. If a field is added to `telemetry_packet_t` (and to `METRICS` in `index.html` per the rule below), add it to `CSV_COLUMNS` too, in the same change — otherwise it'll flow into the database fine but silently never appear in an exported CSV.

### Sessions are bounded by a time gap, not an explicit start/stop signal
`SESSION_GAP_MS` (60000) in `server.js`: a new session starts whenever a packet arrives and either no session is active yet, or the gap since the last recorded packet exceeds this. Nothing in the firmware, `receiver_agent`, or the dashboard explicitly signals "session started" or "session ended" — this is purely inferred from the data flow itself. A receiver briefly losing LoRa sync for under a minute stays in the same session; walking away for an hour and coming back starts a new one. If a different session-boundary policy is ever wanted (explicit start/stop button, vehicle-ignition signal, etc.), that's a real design change to discuss, not a constant to tweak blindly.

### Session views are one-shot renders, not live — don't wire them into the live poll loop
`viewSession()` in `index.html` fetches a session's full point history once and builds Chart.js charts from the complete static array. This is architecturally different from the live Overview/dedicated-tab charts, which grow incrementally via `pushHistory()` on every live poll tick. Don't merge these two code paths — a past session's data doesn't change, and re-polling it repeatedly would be pure waste.

### `POST /api/telemetry` requires the API key; `GET /api/latest` does not
The write endpoint is authenticated (`Authorization: Bearer <TELEMETRY_API_KEY>`) so random internet traffic can't inject fake telemetry. The read endpoint is deliberately public with no auth — it's what the dashboard itself polls, and there's nothing sensitive in a speed/RPM/voltage reading. Don't add auth to `/api/latest`, and don't remove it from `/api/telemetry`.

### `TELEMETRY_API_KEY` must be set as a real Railway environment variable
Without it, `server.js` generates a random key at boot and only prints it to the server log — useless for `receiver_agent` running on someone's laptop, since it has no way to read Railway's log. Set `TELEMETRY_API_KEY` in Railway's dashboard (Variables tab) to a real fixed value, and put the same value in `receiver_agent`'s config. If the key is ever rotated, both sides need updating together.

### `STALE_MS` (2000) defines "offline"
If no POST arrives within this window, `/api/latest` reports `online: false` and the dashboard shows the offline banner, even though `data` still holds the last-known values. The frontend intentionally still displays those stale numbers (not blanked out) alongside the offline indicator — don't change that without a reason, it's useful to see the last known state.

### Dashboard is a single static file, no build step
`public/index.html` is plain HTML/CSS/JS, served directly by `express.static`. Don't introduce a bundler/framework for this unless the UI genuinely outgrows a single file — it deploys as-is with zero build configuration, which matters for how simply this deploys to Railway.

### Live chart history (Overview/dedicated tabs/Multi) is still browser-only and short — that's separate from Sessions
The rolling `history`/`historyTimes` arrays (`MAX_POINTS = 150`, ~30s at 5 Hz) that feed the *live* charts are still client-side-only and still reset on page load — this didn't change. What changed is that there's now a **separate, independent, server-side** long-term record (Postgres) reachable through the Sessions tab, not by extending `MAX_POINTS` or feeding the live charts from the database. Don't conflate the two: the live charts are deliberately cheap and ephemeral; Sessions is deliberately durable and fetched on demand.

### Every chart's x-axis is real wall-clock time — server-stamped, not client-guessed
This was a real bug, fixed in this pass: the live charts used to plot against `historyIndex`, a plain incrementing integer (`pointCount++`) with the x-axis hidden entirely on the mini-charts — not a timestamp at all, on any of them. Two things now make it real time:
1. `server.js` stamps `received_at` (its own `Date.now()`, server receive time) onto the `latest` object in the `POST /api/telemetry` handler — this is intentionally NOT the same value passed to `recordPoint()`/stored in the DB, which gets its own `received_at` from Postgres's `now()` at insert time. Two independent timestamps, two independent purposes (live display vs. durable record) — don't try to unify them into one.
2. The frontend's `historyTimes` array stores that real `d.received_at` (epoch ms), and `applyHistory()` formats it to a locale time string once per update for every chart's labels — mini-charts included, via `BASE_OPTS.scales.x` now defaulting to `display:true` instead of `false`.

If a chart's x-axis ever looks like it's showing sequence numbers or is missing again, check that the data source is actually populating `received_at` — don't quietly reintroduce a counter as a stand-in.

### History points are deduped by `seq`, not by poll tick
`poll()` only calls `pushHistory()`/`pushRawLine()` when `data.seq` differs from the last seen value. This matters because the dashboard polls at the same 5 Hz rate the sender transmits at — without the dedupe check, a single momentarily-slow poll response or a dropped LoRa packet would cause the same packet to be re-fetched and plotted as if it were a new sample, flattening the chart at that point instead of correctly showing a gap. Don't remove this check to "simplify" the polling loop.

### `METRICS` is the single source of truth for every telemetry field shown
Adding a new field to `telemetry_packet_t` that should appear on the dashboard means adding one entry to the `METRICS` object in `index.html` (label, unit, color, decimals, normalization range for the Multi tab, and whether it's on by default there) — the overview mini-charts, dedicated per-metric chart pages, stat cards, and Multi-tab toggle buttons are all generated from that one object, not hand-duplicated per field. Don't add a new metric's markup by copy-pasting an existing tab's HTML; add it to `METRICS` and (if it needs its own tab rather than just showing in Overview/Multi) add the tab markup referencing the same field key so the existing JS wiring picks it up automatically (`ov-<key>`, `big-<key>`, `chart-<key>`, `ovchart-<key>` id conventions). **Also add it to `CSV_COLUMNS` in `server.js`** (see the Sessions rules below) — `METRICS` and `CSV_COLUMNS` are two independent lists that both need updating for a new field to be fully wired end-to-end (dashboard display AND CSV export); a field only in one of them will display but not export, or export but not display.

### GPS/IMU/ESC validity comes from `flags`, not from checking for non-zero values
`gpsValid()`/`imuValid()`/`escValid()` read bit 0 / bit 1 / bit 3 of the packet's `flags` byte (matching `PKT_FLAG_GPS_VALID`/`PKT_FLAG_IMU_VALID`/`PKT_FLAG_ESC_VALID` in the firmware's `config.h` — note bit 2 / `0x04` is the current-sensor flag, not ESC, easy to mix up) rather than guessing validity from whether `latitude`/`roll_deg`/`esc_setpoint_pct` etc. happen to be zero — a real reading can legitimately be zero (e.g. sitting still at a roll of exactly 0°, or the ESC's setpoint genuinely at 0%), so a zero-check would misreport a valid reading as invalid. Only push a GPS point onto the raceline trail when `gpsValid()` is true.

### `.mobile-navbar-tab-primary` needed `padding:0` — a real inherited-class bug, not a design choice
These elements carry BOTH `.tab` (for the shared click-handler/active-sync wiring across desktop tabs, the bottom bar, and the sheet) AND `.mobile-navbar-tab-primary` (its own icon-square look) — `.tab`'s base rule sets `padding:8px 14px` (tuned for a text pill on desktop), and the primary-bar rule never reset it. The result was a real, confirmed visible bug: 14px of horizontal padding vs 8px vertical pushed the centered icon off-balance inside its otherwise-perfectly-square box — more empty space left/right than above/below. Fixed with an explicit `padding:0` on `.mobile-navbar-tab-primary`; flex centering + the icon's own fixed 19px size is all it needs. Verified via `getBoundingClientRect()` insets on all four sides (equal to the 0.5px sub-pixel level) before/after. If a future icon-only square button anywhere else in this file also carries `.tab` for its click wiring, check for this same inherited-padding trap.
**A second, DIFFERENT unevenness bug in the same area, found on follow-up report ("padding still isn't quite even")**: the fix above was symmetry WITHIN one square (icon vs. its own box edges) — this one is symmetry BETWEEN squares. `.mobile-navbar-divider` had `margin:11px 3px` — that 3px horizontal margin stacked ON TOP OF the `justify-content:space-between`-generated gap every pair of flex children in `.mobile-navbar` already gets, making the visible space around the divider (~15.3px, measured) noticeably wider than the space between two icon squares (~12.3px, measured) — small but genuinely visible as "not quite even" padding around the toolbar. Fixed by dropping the divider's margin to `11px 0` (vertical margin unchanged — that's what gives it its height; only the horizontal component was the bug) — all seven gaps across the bar (edge→first icon, icon→icon ×4, icon→divider, divider→expand, expand→edge) now measure equal via `getBoundingClientRect()`, confirmed directly, not eyeballed. If a future element is added between flex children in `.mobile-navbar`, don't give it its own horizontal margin unless it's deliberately meant to sit off-rhythm from `space-between`'s automatic spacing — that's exactly what caused this.
**A THIRD unevenness bug found on the same report, in `.mobile-navbar-expand` specifically**: unlike the icon squares (plain `<div>`s), this is a real `<button>` element, and browsers give buttons non-zero default padding (Chrome: ~1px 6px) that was never reset here — same category of bug as the tab-square padding fix above, different source (browser UA stylesheet, not an inherited site CSS class). Fixed with an explicit `padding:0`; the two overlaid icon spans (`position:absolute`, no inset values, centered via the parent's flex `align-items`/`justify-content`) only land correctly once the button's own box isn't skewed by leftover default padding. Verified: 42×42 button, all four icon insets equal to the pixel.

### Onboarding overlay's Android-nav-bar-matching background has to live in the LATER mobile media block, not the one `#app`'s own rule sits in
A real bug caught and fixed: `.onboarding-overlay`'s system-bar-matching gradient (see the rule below) was first added inside the SAME early `@media(max-width:860px)` block `#app`'s own gradient rule lives in (for comment locality) — but an unconditional (non-media-scoped) `.onboarding-overlay { background:var(--bg0) }` base rule sits further DOWN in the stylesheet than that early block, so by plain CSS cascade order (equal specificity, later source position wins) the unconditional rule silently overrode the gradient on every viewport, including mobile. Moved into the file's OTHER, later `@media(max-width:860px)` block (the one with `.settings-mobile-only`, `#page-overview` mobile rules, etc.) — which sits after the base rule's position — fixes it. Verified via `getComputedStyle().backgroundImage` actually returning the gradient on a mobile viewport, not just eyeballing the CSS. If any future mobile-only override for a selector that ALSO has an unconditional base rule doesn't seem to be applying, check which of this file's two `max-width:860px` blocks it landed in relative to that base rule's own position — this is a real, repeatable trap in a single-file stylesheet this large, not a one-off.

### Onboarding / access gate — required password, then a platform-specific optional install step, then a feature overview, then a Settings tour
`#onboarding-overlay` (`z-index:200`, above literally everything else including the required-update banner's 60 AND — see the confirm-modal z-index note below — everything except a confirm-modal opened from within it) shows on first visit, both desktop and mobile, before any dashboard content is usable. Steps, in order: **password** (both platforms — a plain-text comparison against `ONBOARDING_PASSWORD`, `'SentinelRacing26!'`, hardcoded client-side; see the note below on what that does and doesn't mean security-wise) → **install** (mobile only) *or* **agent** (desktop only) — `onboardingSteps` is `IS_MOBILE ? ['password','install','overview','settings'] : ['password','overview','agent','settings']`, so each platform gets exactly one relevant, NOT required, skippable companion-install step, in a different position in the sequence (mobile promotes installing the app itself right after the password; desktop shows the general feature overview first, THEN offers the receiver agent, since agent installation only matters to someone actually running a receiver — see the "agent" step's own note) → **overview** (both — a short list of feature bullets) → **settings** (both, the final step — either "Open Settings" (finishes onboarding AND switches straight to the Settings tab) or "Skip — Go to Dashboard" (finishes onboarding, leaves whichever page was already active)). `ONBOARDING_KEY` (`gp_onboarding_v1_complete`) is checked once at load; if unset, the overlay shows and the step index starts at 0.
**Step switching is animation-based, not a display↔opacity crossfade** — `.onboarding-step-active` (added to exactly one step at a time by `showOnboardingStep()`) both sets `display:flex` AND carries a one-shot `@keyframes` entrance animation (`onboardingStepIn`); a `display:none`→something toggle has no valid "before" state for a `transition` to animate from (the same documented gotcha `.mobile-navsheet` ran into elsewhere in this file), but a CSS *animation* has no such requirement — it just starts playing the instant the element enters the render tree with the class already on it. Simpler and safer than a real crossfade for an access-gate flow regardless: JS only ever marks ONE step active, so there's no risk of two steps overlapping mid-transition or a "stuck between steps" state. The overview step's feature bullets are individually staggered (`nth-child` animation-delay) for a bit of extra polish. Primary "move forward" buttons (`.onboarding-btn-primary`) use the same solid accent gradient (`linear-gradient(135deg, var(--accent), var(--accent2))`) install-fab/active tabs already use elsewhere, not the outlined `.btn-accent` style — this is the one recurring "important, colorful" action on every step.
**The desktop "agent" step opens the REAL `#download-confirm-modal`** (`showDownloadConfirm()`, the same function the header's Download Agent button calls) rather than duplicating that flow — which is why `.confirm-modal`/`.confirm-modal-backdrop`'s z-index had to move from 30/31 to 210/211, above `.onboarding-overlay`'s 200: a confirm-modal opened from within onboarding needs to render on TOP of it, not disappear behind it. **Both halves of that pair must move together** — a real bug hit while building this: bumping only the backdrop's z-index above the modal's own (unchanged) 31 made the backdrop itself intercept clicks meant for the modal's buttons (verified via `elementFromPoint` landing on the backdrop, not the Download button) — confirmed and fixed before shipping, both now at 210/211. An actual click-through on `#download-confirm-go` while the "agent" step is active also auto-advances onboarding (a listener added alongside the modal's own existing `hideDownloadConfirm` one, not replacing it) — canceling out of the modal instead leaves the user back on the "agent" step, where its own separate Skip button still works.
**The password field is `type="text"` with `-webkit-text-security:disc` CSS masking, not `type="password"`** — a real fix, not a style choice: Chrome's save-password infrastructure keys specifically on `input[type=password]` (`autocomplete="off"` is documented to NOT reliably suppress it there), and this field isn't a real account credential, so it's better off never registering as a password-manager candidate at all. Visually identical (dots) in Chrome/Safari/Edge; Firefox lacks `-webkit-text-security` support and shows plain text there — an accepted trade-off given the code is already plainly readable in this file's own source anyway (see the note below).
**Gradient direction on wide-but-short elements must be `to right`, not `135deg` — a real bug, found on "looks bright blue, not a gradient" report.** `.oui-toggle-track` (checked state), `.onboarding-btn-primary`, and `.mobile-navsheet-tab.active` all used `linear-gradient(135deg, var(--accent), var(--accent2))`, copied from `install-fab`/the mobile bar's active-tab highlight — both of which are roughly SQUARE, where a 135° diagonal genuinely spans corner to corner. On an element much WIDER than it is tall (a 50×30px toggle track, a full-width ~40px-tall button, a wide sheet row), a diagonal gradient's color transition completes over the distance perpendicular to its angle — close to the short (height) dimension, not the width — so the blend finishes almost immediately and most of the visible width just showed solid `var(--accent2)`, reading as "bright blue" rather than an actual two-color gradient. Fixed by switching all three to `linear-gradient(to right, ...)`, which spans the full width by definition.
**`.oui-toggle-track`'s checked-state gradient needed TWO follow-up fixes on top of the direction change, each catching what the previous one missed.** (1) "cuts off on the right and left end" — the direction fix alone was correct (verified live: the deployed site really was serving `to right`), but the OPAQUE WHITE THUMB sits on top of the 50px-wide track and, when checked, covers exactly its 23px-45px span (`left:3px` + `translateX(20px)` = 23, thumb width 22px) — leaving only two thin exposed strips (0-23px left, a mere 5px sliver 45-50px right). A plain evenly-spread 2-stop gradient means that 5px right sliver only samples the very tail of the blend, nowhere near pure `var(--accent2)` — reading as the color "cutting off". First fix: 4-stop percentages at `0%/40%/92%/100%`, chosen to be roughly (not exactly) inside the thumb's footprint. (2) "still a razor thin gradient color mismatch" — that first fix's stops (40%/92%) were APPROXIMATE, not aligned to the thumb's real edges (46%/90% exactly, i.e. 23px/45px of 50px) — leaving a real ~1px band (20-23px on the left tail, 45-46px on the right) that was still mid-blend, not yet pure color, sitting immediately next to the thumb as a visible thin off-color seam. Fixed by setting the solid-color boundaries to the thumb's EXACT pixel edges (`var(--accent) 0%, var(--accent) 46%, var(--accent2) 90%, var(--accent2) 100%`) — every visible (non-thumb-covered) pixel is now provably 100% pure accent or accent2, with the entire blend confined to exactly the region the thumb covers, where it's invisible either way. **When aligning a gradient's solid zones to sit outside a covering element, compute the exact pixel/percentage boundary — "roughly outside it" still leaves a thin visible seam at the boundary itself.** (3) Even the exact-pixel version (46%/90%) was still reported as showing "a razor thin gradient color mismatch" — precision stop-tuning was fundamentally fragile against sub-pixel rendering/rounding, which can shift a soft blend's edge by a fraction of a pixel independent of how exactly the percentages are computed. Per explicit instruction to rework rather than re-tune, replaced the whole approach with a **hard-edge, zero-blend gradient** using CSS's double-position stop syntax: `linear-gradient(to right, var(--accent) 0% 50%, var(--accent2) 50% 100%)` — both colors stop at the identical 50% position, so there is no interpolation distance at all, i.e. no blended pixel anywhere in the gradient for a rendering discrepancy to expose, regardless of where the thumb sits. Verified via `getComputedStyle` that the browser normalizes this to two stops at the same 50% position (confirming true zero-width transition), not just visual inspection. **When precision-tuning a gradient's stop positions to hide a seam has already failed twice, the fix is usually to eliminate the blend/transition entirely (hard edge) rather than attempt a third, even-more-precise version of the same soft-blend approach.** (4) That hard-edge rework was itself wrong — misread as an implicit request to hide the transition, when the actual ask the whole time was a **smooth blend visible across the entire track, thumb included**, not a seam-free edge. Reverted to a plain unweighted 2-stop `linear-gradient(to right, var(--accent), var(--accent2))` spanning the full 0%-100% width — the "cuts off"/thumb-occlusion analysis in (1)/(2) was real (the thumb genuinely does cover most of the track), but that's an accepted, intended side-effect of a smooth background gradient sitting behind a solid thumb, not a bug to engineer around. **Before iterating further on a visual "seam"/"mismatch" complaint, confirm what the end state is actually supposed to look like (smooth blend vs. hard split) rather than assuming the goal is to make a transition invisible.** (5) Follow-up ask: "make the width of the gradient just a few pixels wider on each side" — on the reverted plain 2-stop version the blend starts immediately at 0%/100%, so neither end ever reads as fully its own color. Fixed by holding a few px of pure color at each end before the blend begins: `linear-gradient(to right, var(--accent) 0%, var(--accent) 8%, var(--accent2) 92%, var(--accent2) 100%)` (8%/92% of the 50px track ≈ 4px each side) — still a real smooth blend across the middle, just with solid color margins at the ends instead of the blend touching the edge. (6) "make it even wider" — widened again to `0%/20% ... 80%/100%` (~10px each side), same technique, just a bigger solid margin and a narrower middle blend band. (7) "dont make the switch wider keep it the same just stretch the gradient background out behind it" — clarified that (5)/(6) were the wrong shape of fix: solid-color stops still keep the gradient CLIPPED to the track's own fixed 50×30 box, they just hold the ends static-colored inside it — not what "stretch it out" meant. Moved the gradient onto a `::before` pseudo-element that is wider than the track (`left:-10px; right:-10px`, same `border-radius:15px`) and sits behind it, so the color genuinely bleeds past the switch's own edges while the switch's clickable/visual size stays exactly 50×30. Track's own background goes `transparent` when checked so the pseudo shows through; deliberately left off `z-index` on the pseudo at first (tried `z-index:-1` alone, reverted) since a bare negative z-index on the pseudo would place it in the TRACK'S PARENT's stacking context, risking it painting behind the row's own card background instead of just behind the thumb; plain DOM/paint order already puts `::before` (painted first) behind the thumb (painted after) with no stacking-context risk. (8) Follow-up: "make it only 5px wider... and put it back behind the box" — shrank the bleed to `-5px`/`5px`, and to make "behind the box" explicit/robust rather than relying on implicit paint order, added `isolation:isolate` on the track itself (giving IT a stacking context) plus `z-index:-1` back on the pseudo — now the negative z-index is safely contained to *within* the track's own stacking context (behind the track/thumb only) instead of leaking into the track's parent's context the way a bare `z-index:-1` would. (9) "shrink it another 5px on each side so its even" — 5px minus another 5px is 0, so the pseudo's `left`/`right` went from `-5px` to flush `0`: the gradient layer is now exactly the same size as the track (even with the switch's own edges, no bleed), just still architecturally a separate `::before` layer behind the track rather than the track's own `background` — kept that separation (instead of collapsing back to a plain `background` on the track) so the `isolation`/`z-index` stacking-safety from (8) stays in place if a future ask reintroduces bleed.
**`.mobile-navbar-tab.active` (the toolbar's own active-icon square highlight) needed the same direction fix too, on a follow-up screenshot** — this one was deliberately left at `135deg` on the reasoning that a 42×42 square is symmetric enough for a diagonal not to matter. A screenshot showed that assumption was wrong in practice (still visibly more indigo on one side, teal on the other, even on a near-square box) — switched to `to right` too, for consistency with every other gradient fill in this file now. **If a future gradient fill is added to a wide/short OR square element, default to `to right` (or `to bottom` for tall/narrow) — `135deg` turned out not to be safe even on the one "roughly square" case tried here**; `install-fab`'s circular button is the only remaining `135deg` use in this file.
**`.onboarding-btn-secondary` (the Skip buttons) were missing the base `.btn` class in markup** — a real bug, not a deliberate "grey" look: without `.btn`'s reset, they rendered as unstyled native browser buttons (Chrome's default grey-filled box), which is what read as "the grey buttons ... not good." Fixed two ways together: added `.btn` (+ `.btn-sm`) back to the markup, and gave `.onboarding-btn-secondary` its own explicit outline-only style (`background:transparent`, `border:1px solid var(--border)`) instead of relying on any fill — reads clearly as the secondary/optional action next to the primary button's solid gradient fill.
**This is a UI speed-bump, not real access control** — the password check happens entirely in client-side JS against a hardcoded string anyone can read directly from this file's source (view-source, or just grep it), and every actual API endpoint (`/api/latest`, `/api/sessions`, etc.) remains exactly as public/unauthenticated as it already was (see the `/api/latest` auth rule elsewhere in this file — `POST /api/telemetry` is the only endpoint with real auth, via `TELEMETRY_API_KEY`, and that's unrelated to this gate). Don't describe this to a user as "securing" the dashboard's data — it only gates the dashboard's own UI from a casual visitor, nothing more. If real access control is ever wanted, that's a server-side auth mechanism to design, not a JS string comparison.
**The install step mirrors, but deliberately doesn't share code with, the existing `install-fab`/`checkIosInstallHint()` logic** (same three cases: real `beforeinstallprompt` captured in `deferredInstallPrompt`, iOS with no such API — Share-sheet instructions instead — or already running standalone) — the two UIs (a floating popover vs. a full-screen onboarding step) render differently enough that sharing the DOM-manipulation code wasn't worth the coupling; the underlying `deferredInstallPrompt` variable and `hideInstallUi()` function ARE reused directly, though, since those are the actual install-triggering plumbing, not UI.
**The install step waits for the real install to complete, and a fresh launch of the newly-installed app resumes at the overview step (page 3) instead of restarting** — per explicit request, accepting the install prompt does NOT `advanceOnboarding()` immediately; it swaps the step into a "waiting" state (button/skip hidden, desc text changed) and only sets `ONBOARDING_INSTALL_DONE_KEY` when the real `appinstalled` event fires. That event confirms the OS finished installing — it does NOT mean the user has actually opened the new app yet, and this browser tab has no way to observe that at all (it's a separate app process/window). So the actual "move to page 3" happens on the NEXT page load instead: the init logic at the bottom of this section checks `IS_MOBILE && isRunningStandalone() && ONBOARDING_PASSWORD_PASSED_KEY && ONBOARDING_INSTALL_DONE_KEY` and, if all true, starts `onboardingStepIndex` at `onboardingSteps.indexOf('overview')` directly rather than 0 — since `localStorage` is shared origin storage between the browser tab and an installed WebAPK for the same site, these progress flags survive the install. A plain reload of the SAME (non-standalone) tab after only the password step resumes at `install` (index 1) instead of restarting at the password prompt either. iOS has no `appinstalled`/`beforeinstallprompt` API at all, so this wait-and-resume behavior simply isn't possible there — the button stays hidden and Skip is the only path, unchanged from before.
**A real intermittent bug, reported as "sometimes it just skips the install screen"**: the click handler used to do `if (!deferredInstallPrompt) { advanceOnboarding(); return; }` — treating "no captured prompt yet" exactly like "the browser decided install isn't available", and silently skipping forward either way. But `beforeinstallprompt` fires asynchronously sometime after page load with no fixed timing guarantee — a user who races through the password step and taps Install almost immediately can genuinely reach this code before the event has fired, and from their side that looked exactly like a random, unexplained skip. Fixed by NOT auto-advancing on a missing prompt — it now sets the description to `"Install isn't ready yet — wait a moment and try again, or tap Skip to continue."` and leaves the step exactly where it is, so a legitimately-unavailable prompt and a not-yet-arrived one are both handled honestly instead of guessing which case it is.
**"Clear all cookies for this update" was handled two ways**, not one: (1) `ONBOARDING_KEY` is a brand-new name, never used by any earlier version of this file — that alone guarantees every existing visitor (not just genuinely new ones) sees the new gate on their next visit, since nobody has this key set to `'1'` yet; (2) a one-time migration (`localStorage['gp_cookies_cleared_v1']`, checked/set once) additionally clears any real browser cookies, purely defensive since this app has never actually set one itself (everything else here is `localStorage`) — the request was specifically about cookies, so this doesn't also wipe other already-saved preferences (range/haptics/overview-cols etc.) for returning users the way the Settings button below deliberately does.
**Settings → Clear Website Data** (`clearAllWebsiteData()`, behind its own `#clear-data-confirm-modal` — reuses the same `.confirm-modal` markup/CSS as the download-agent and exit-app confirmations, a third independent instance of the same pattern) is the ongoing, user-facing, MUCH more thorough version: `localStorage.clear()` + `sessionStorage.clear()` + clears any real cookies + unregisters any service worker + deletes any Cache Storage entries, then `location.reload()`. This wipes `ONBOARDING_KEY` along with everything else, so a reload after clearing correctly shows the onboarding gate again — that's intentional, not a side effect to guard against.

### Exit confirmation on Android back gesture — "press back again to exit", NOT a modal — `window.history`, never bare `history`
This is a single-page app with no real navigation (tabs are pure JS class-toggling, no URL/hash changes ever happen), so a plain page load leaves only ONE entry in the browser's history stack — on an installed Android WebAPK, a back gesture/button press from there has nothing to go back TO, so Android just closes the app outright with no chance to intercept it. `window.history.pushState({gpExitGuard:true}, '')` seeds one spare entry on load.
**REWRITTEN from an earlier "Close App" modal, which never reliably worked** — that version needed a SECOND `history.back()` step to be triggered from script once the user tapped confirm, and none of three attempts (`history.go(-2)` — a real bug, targets an out-of-range index and is a silent no-op per the History API spec; two real sequential `history.back()` calls, which DID fire two genuine navigation events but still didn't close the real device's WebAPK; adding `window.close()` as a third attempt) reliably closed the app on the actual installed device. The likely reason: Android/Chrome's "finish the Activity when history is exhausted" decision appears tied to the genuine, OS-observed back gesture/button itself, not purely to the resulting history-stack state — a JS-simulated `back()` that reproduces the identical end state isn't guaranteed to trigger the same OS-level decision a real hardware press does. **There is no universally-documented, guaranteed JS API for a page to close an installed PWA on Android** — browsers deliberately restrict "close yourself" for security/UX reasons.
**The fix: stop simulating the second press in script — let a genuine second real back press do it.** The first real `popstate` (from consuming the guard) shows `#exit-toast` ("Press back again to exit", a brief non-blocking snackbar, NOT a modal — no button, no decision required) and, critically, does **NOT** immediately re-push the guard the way the old version did. It arms `exitRearmTimer` (`EXIT_REARM_MS`, 2500ms) instead. If a SECOND real back press comes within that window, there's genuinely nothing left in the session's history for it to consume — Android handles that directly, no `popstate` even fires, no JS involved at all — the exact same real, reliable mechanism that closes any other installed WebAPK (this is literally the original "just closes with no warning" behavior this whole feature exists to guard, now deliberately allowed through on a confirmed SECOND press). If no second press comes in time, the timer re-arms (pushes a fresh guard) so a later stray back tap doesn't unexpectedly land one gesture away from exiting. Verified directly: `history.length` stays unchanged and `history.state` becomes `null` (the true pre-guard entry) after the first back press — confirming no premature re-push — then becomes `{gpExitGuard:true}` again after the rearm delay.
**A real bug, caught before shipping, not a style nit**: this script already has a top-level `const history = {}` (the live-chart data buffer — see `MAX_POINTS`/growing-buffer rules elsewhere in this file) declared LATER in the same script. A bare `history.pushState(...)` reference textually BEFORE that `const` still resolves to that same script-scoped binding (not the global `History` object) due to how `const`/`let` hoisting works — and since it's accessed before its own initializer runs, it throws `ReferenceError: Cannot access 'history' before initialization` **and this halts the ENTIRE rest of the script's execution**, silently breaking everything below it (polling, chart rendering, every other feature) on every single page load. Caught via `read_console_messages` before this reached the user — every reference to the real browser History API in this file MUST use `window.history`, never bare `history`. If a future edit ever needs the real History API here, check for this shadowing first.

### Overview Boxes to Show has a "can't deselect the last one" guard, and a non-obvious default set
The `change` handler on each checkbox in `#settings-ov-mobile-list` refuses (snaps the checkbox back to checked, doesn't save/apply) if unchecking it would leave zero visible mobile-Overview tiles — an empty Overview page with nothing left to tap would have no visible way back to turning any back on. Separately, `OV_MOBILE_DEFAULT_ON` (a `Set`) defines which of the (now 16) items start ON before any `localStorage` override exists: **Speed, Battery, Current, Temp, Wheel RPM, Consumed Wh, Remaining Wh, Efficiency (Wh/mile)** (8 total) — Motor Volt/Motor RPM/Packet #/Roll/Pitch/Yaw/GPS/Link start OFF. This was an explicit request, not a guess at "sensible defaults" — don't reorder/change this set without being asked again. **Updated once already**, per an explicit follow-up: the original 8 was Speed/Battery/Motor Volt/Current/Temp/Motor RPM/Wheel RPM/Packet #; Motor Volt/Motor RPM/Packet # were named specifically to be swapped OUT in favor of the 3 new Efficiency stats (not just "add 3 more" on top of 8) — see the Efficiency-on-Overview rule below for the full reasoning. All three swapped-out keys are still fully present in `OV_MOBILE_ITEMS`/the customize list, just default-OFF now — nothing was deleted from the code, only from the shipped default.

### App update check — `/api/version` (server) + `checkAppVersion()` (client) — REQUIRED, not dismissible
`server.js` computes a SHA-1 hash of the served `public/index.html`'s content once at boot (`APP_VERSION`) and exposes it via `GET /api/version` (public, no auth — same reasoning as `/api/latest`). Deliberately a computed hash, not a hand-maintained version string — nothing to remember to bump, and it changes if and only if the page actually did on a real deploy. The client's `checkAppVersion()` fetches this with `cache:'no-store'`, treats the FIRST successful fetch as the baseline (`appVersion = d.version`, no banner), and calls `showUpdateRequired()` the moment a later fetch returns a different hash. Runs once on load, every 5 minutes via `setInterval`, and again on `visibilitychange` back to `'visible'` — that last one matters most in practice, since an installed PWA sitting backgrounded for hours (missing a deploy entirely on the timer) is the actual scenario this exists for, more than someone leaving a browser tab open and staring at it.
**`#update-banner` has no dismiss button, and `#update-banner-backdrop` blocks interaction with the rest of the page while it's showing** — per explicit request, this is required, not a light notice. The backdrop is `position:fixed; inset:0; z-index:59` (just under the banner's own `z-index:60`, but above the mobile nav bar/sheet's 14/15 and everything else) so it genuinely intercepts clicks/taps on anything behind it, not just visually dims it — verified via `document.elementFromPoint()` (real hit-testing) landing on the backdrop at a tab's coordinates, not the tab itself; a plain `.click()` call in a test is NOT a valid way to verify this, since programmatic `.click()` bypasses hit-testing/z-index entirely and fires regardless of what's visually on top. `#update-banner-reload` (still hittable — it sits above the backdrop) just calls `location.reload()` — no service worker to bypass, since `sw.js` never caches anyway (see its own rule), so a plain reload always gets the current file. If this ever needs to become dismissible again, that's a real behavior change to confirm, not a default to fall back into.

### Orientation lock is three layers, only one of which is airtight
A website fundamentally can't override the phone's OS-level auto-rotate setting from JS alone — this was a real ask ("prevent the user from rotating their phone screen"), and the honest answer is "reach for every real lever, but only one of them is guaranteed." (1) `manifest.json`'s `"orientation":"portrait-primary"` (was `"any"`) is the actual, reliable lock — but ONLY for an installed PWA/WebAPK launched in standalone display mode, and even then only on a *fresh* install (an already-installed copy needs uninstall+reinstall, same manifest-caching limitation as the app-name/theme-color fixes noted elsewhere in this file). (2) `screen.orientation.lock('portrait')` (mobile-only, wrapped in try/catch, silently no-ops on failure) is a second attempt aimed at that same standalone case — most browsers reject it outside Fullscreen mode, which this page doesn't use, so treat this as a bonus, not something to rely on. (3) `.landscape-lock-overlay` is the only layer that reaches EVERY remaining case (a plain, not-installed mobile browser tab, or an already-installed copy that hasn't been reinstalled yet) — pure CSS, `@media (max-width:860px) and (orientation:landscape)`, no JS involved so it can't silently fail: a full-screen "rotate back to portrait" message, since none of this file's mobile-specific layout was ever designed to support landscape.

### `#gps-map` needs `isolation:isolate` or Leaflet paints over the mobile toolbar
Real bug, not a Leaflet misconfiguration: Leaflet's internal panes (tile/overlay/shadow/marker/popup) use `z-index` values up to ~700, and `.leaflet-container` doesn't establish its own stacking context by default. Without `isolation:isolate` on `#gps-map`, those high `z-index` panes compete directly in the PAGE's root stacking context against other fixed-position UI — specifically `.mobile-navbar` (`z-index:15`) — and win, painting the map on top of the toolbar despite being much earlier in the DOM and visually "behind" it in every other sense. `isolation:isolate` confines every Leaflet `z-index`, however high, inside `#gps-map`'s own stacking context — only `#gps-map`'s own position in the page's stacking order matters to anything outside it after that. If a future map/widget embed shows the same "floats over fixed UI it shouldn't" symptom, this is the first thing to check — it's almost always a missing stacking-context boundary, not a z-index number that needs to go higher.

### The raceline map auto-zooms to fit the recorded trail, not just a fixed-zoom follow
`updateGpsMap()` used to always either `setView(..., 17)` (first fix) or `panTo()` (every fix after, same zoom forever) — the car could easily wander outside a fixed zoom-17 view with no automatic way back to seeing the whole route short of a manual zoom-out. Now: on every fix after the first, if the new point falls outside the map's current visible bounds (`!gpsMap.getBounds().contains([lat,lon])`), it calls `gpsMap.fitBounds(gpsPolyline.getBounds(), GPS_FIT_OPTS)` instead of `panTo()` — this re-zooms/pans to fit the WHOLE trail collected so far, not just the new point, so the map keeps the full route visible as it grows rather than cropping to wherever the car currently is. Points still inside the current view just `panTo()` as before (no unnecessary re-zoom while nothing's actually left the frame — a user who's manually zoomed in to inspect part of the trail doesn't get yanked back out every 200ms for a point that's still on screen). `GPS_FIT_OPTS` (`padding:[30,30]`, `maxZoom:19`) is shared with the tab-switch handler, which was also changed to `fitBounds()` the whole trail (when there are 2+ points) instead of just re-centering on the last known point at whatever zoom the map happened to have — arriving on the Raceline tab now shows the full route at a glance. Leaflet's `fitBounds()` picks the largest whole zoom LEVEL that fits the padded bounds, so the literal requested bounds can end up a few percent outside the final viewport at some zoom steps (normal Leaflet quantization, not a bug) — the visible zoom/pan direction is still always correct.

### ESC mode/state are strings, not `METRICS` entries
`esc_mode`/`esc_state` are handled by hand in `applyState()` (the `esc-mode`/`esc-state` elements), not added to the `METRICS` registry — `METRICS` assumes every field is a chartable number with decimals and a normalization range, which doesn't apply to a string like `"NORMAL"` or `"RAMP"`. The three numeric ESC fields (`esc_setpoint_pct`/`esc_live_pct`/`esc_ramp_pct`) *are* in `METRICS` and get the full auto-wired treatment (chart, history, Multi-tab toggle) for free. If a future string-valued field needs display, follow the mode/state pattern, not the `METRICS` pattern.

### Colors are CSS custom properties, resolved at chart-creation time
Chart.js needs real color strings, not `var(--foo)` references — `resolveColor()` reads the computed CSS variable once when each chart is built. If the theme's CSS variables are ever changed dynamically (e.g. a light/dark toggle) rather than just at load time, charts built before the change won't pick up new colors without being recreated — this isn't wired up for live theme switching.

### Mobile is a layout mode of the same page, not a separate build
Below `860px` wide, a `@media` block hides the desktop `#tabs` bar and shows `.mobile-sidebar` instead (fixed-position icon rail, `56px` collapsed / `210px` expanded via a `.expanded` class toggle on click), and hides `#overview-right-wrap` (the Overview graph grid) — mobile Overview shows only the stats-bar numbers, never the charts, per an explicit design choice to keep the mobile Overview lightweight. All 12 tabs (`overview`/`speed`/`power`/`temperature`/`rpm`/`esc`/`imu`/`raceline`/`link`/`multi`/`sessions`/`raw`) are duplicated as `.mobile-tab` elements with the same `data-page` values as the desktop `.tab` elements, and the shared click handler keeps both sets' `.active` class in sync — don't add a new tab to only one of the two tab lists, or that page becomes unreachable from whichever UI mode was skipped. Selecting a mobile tab also calls `collapseMobileSidebar()` so the rail snaps back to icon-only after navigating.

### No CSS `transition` on the mobile sidebar's width/label — same reason as the stats-bar collapse
`.mobile-sidebar.expanded` and `.mobile-tab-label` change `width`/`display` instantly, with **no** `transition` property. This mirrors the earlier, deliberate fix to the Overview stats-bar collapse (`.stats-row{display:none}` instead of an animated `max-height`/`grid-template-rows`): CSS-transitioned state changes on this page have repeatedly failed to reliably reach their declared end-state, while instant class-driven toggles have not. If a future edit wants to animate the sidebar, verify the end-state actually applies (computed width, not just class presence) before trusting it — don't re-add a bare `transition:` and assume it works.

### PWA installability — manifest + service worker, deliberately no offline caching
`public/manifest.json`, `public/sw.js`, and the icon SVGs exist solely to satisfy the browser's install criteria (Chrome/Android requires a registered service worker with a `fetch` listener before firing `beforeinstallprompt`). `sw.js` intentionally never calls `event.respondWith()` — this is a live-telemetry dashboard, and caching API responses or the page shell risks silently serving stale readings, which would undermine the entire point of the tool. Install is offered via a small floating button (`#install-fab`), not a banner — see the icon/install rule below. If real offline behavior is ever wanted, that's a deliberate feature to design (e.g. an explicit "stale/offline" state), not something to fall into by adding a cache to `sw.js`.

### Install prompt is a floating icon button, not a banner
`#install-fab` (bottom-right, circular, hidden unless `.show`) replaced an earlier full-width banner — a banner permanently claims header space most visits don't need; the FAB costs nothing until tapped, then opens `#install-popover` (small card, `.show` to display) with the actual "Install" action (Chrome/Android, via the captured `beforeinstallprompt`) or manual instructions (iOS, no such API exists there). Dismissal still persists via the `gp_install_dismissed` localStorage key. **Caught once in testing, worth remembering**: `.install-fab`'s own base rule must never set `display:` itself — the page-wide default-hidden rule (`.mobile-sidebar, .mobile-sidebar-backdrop, .install-fab, .install-popover { display:none; }`) and `.install-fab`'s own base rule are equal-specificity single-class selectors, so whichever is declared later in the stylesheet wins the cascade regardless of the `.show` class; `display:flex` must live only on the `.install-fab.show` rule.

### Icons are inline SVG from one JS map, not emoji
Every static UI icon (nav tabs, header logo, sidebar toggle, hide/customize/refresh/export/close/clear/install buttons) is an empty `<span data-icon="name">` in the HTML, filled in once at load by `applyIcons()` from the `ICONS` map near the top of `<script>` — add a new icon by adding one entry to `ICONS` and one `data-icon="..."` span, not by pasting SVG markup inline. Deliberately not emoji: emoji render inconsistently across platforms/fonts/OSes and read as informal; one hand-authored Feather-style icon set (stroke=`currentColor`, no fill) reads as a coherent design system and automatically inherits whatever text color its container already has. Dynamic runtime status glyphs (e.g. the GPS fix ✅/❌ strings written by `applyState()`) were deliberately left as-is — converting those would mean restructuring the DOM nodes they get written into, and they're data indicators rather than navigation/action iconography.

### Chart axes are pre-filled and range-seeded so a fresh chart doesn't visibly lurch on its first real data points
`history[k]` starts as `MAX_POINTS` nulls (not an empty, growing array) and `historyTimes` is seeded with `MAX_POINTS` backdated placeholder timestamps spaced `SENSOR_INTERVAL_MS_GUESS` (200ms) apart, both filled in before the first `applyHistory()` call (which now also runs once immediately on load, not only after the first live packet). `safeChart()` additionally seeds `scales.y.suggestedMin/suggestedMax` from each metric's `METRICS[k].range` when a `metricKey` is passed. Together this means every chart already shows its full time window and a sane y-range from the very first paint — without it, a brand-new chart auto-fits tightly to whatever 1-2 real points exist so far, and every subsequent point yanks the axes into a new shape until `MAX_POINTS` is finally reached (this was the "graphs expand rapidly when first receiving data" bug). If a new metric/chart is added, pass its `metricKey` through to `safeChart()` — omitting it just means that one chart's y-axis won't get the stabilizing hint, not a hard failure.

### Tooltips work on both hover (desktop) and tap (mobile) — this requires `intersect:false`
Every chart's line has `elements.point.radius:0` (no visible dots — a deliberate look), which means Chart.js's *default* tooltip trigger (`intersect:true`, i.e. "the pointer must land exactly on a rendered point") can never fire — there's no point to land on. `BASE_OPTS` sets `interaction:{mode:'index', intersect:false}` (and each per-chart `options` object needs the same, done in the multi-chart and session-chart config too, since they don't inherit from `BASE_OPTS` directly) so the tooltip instead fires from proximity to an x-position — which is what makes it work as a touch/tap gesture on mobile as well as a mouse hover on desktop, since Chart.js's default `events` list already includes `touchstart`/`touchmove` needing no separate mobile-specific code path. `TOOLTIP_OPTS` is a shared object so all chart types render tooltips with the same dark, theme-matched styling.

### The mobile sidebar uses a real, animated CSS `transition` on `width` — and how to verify that, since the obvious way is broken here
Earlier work concluded CSS transitions in this project "never reach their end state" and switched several UI pieces (the stats-bar collapse, once the mobile sidebar itself) to instant `display`/class-driven toggles instead. Re-investigated during the icon/animation pass: the real cause is specific to the automated browser-preview tool used to verify this page in this dev environment — its browser pane only composites frames (runs `requestAnimationFrame`, applies CSS transitions) while actually displayed/focused; when it isn't, `computer{action:"screenshot"}` fails outright with "the Browser pane is not displayed, so the page is not compositing frames", and under that same condition **any** CSS transition — even a trivial transform on a freshly-created throwaway element — silently never reaches its end state no matter how long you `await`, while forcing `el.style.transition = 'none'` on the same class toggle applies the end state instantly and correctly. That's a property of the verification tool, not of real browsers or of CSS transitions — a normal, visible/focused browser tab always composites and transitions animate normally for real users. **When verifying a CSS transition/animation change in this environment**: don't trust `getBoundingClientRect()`/`getComputedStyle()` read immediately or after an `await setTimeout` — instead temporarily set `el.style.transition = 'none'` before toggling the class, confirm the computed end-state is structurally correct, then remove the inline override; that isolates "is the class/selector/rule correct" (which the tool *can* verify) from "does it animate" (which only a real, visibly-displayed browser can show). Don't strip a `transition` from this file's CSS again based on a synchronous/async-timeout check failing to show the end state — check it this way first.

### Test Mode is a client-side-only fake-data generator, never touches the network
The `#test-mode-btn` toggle in the header runs `genTestPacket()`/`testModeTick()` (near the bottom of `<script>`), which pushes synthetic packets through the *exact same* `applyState()`/`pushHistory()`/`applyHistory()`/`pushRawLine()` functions a real `/api/latest` response would — this is deliberate, so testing the UI can't silently drift from how real data actually renders. It never calls `fetch`/`POST` anywhere, so it can't pollute a real session or write to the database. `poll()`'s own `setInterval` checks `testModeActive` and skips itself while test mode is on, handing back to the real feed the instant it's turned off (an immediate `poll()` call, not waiting for the next 200ms tick).
**Values are a damped random walk (`stepTestWalk()`), not a sine wave** — per explicit request ("more random, fluctuate slower and less consistent, closer to real data"). The original generator (`wave()`) was a clean per-metric sine curve on a shared/derived clock — smooth, but exactly periodic (watch one metric for its ~6-15s period and you'd seen its entire future shape) and nothing like how real sensor data actually behaves. `testWalkState[key] = {value, velocity}` (seeded at each metric's range midpoint) is a discrete damped random walk: a proportional mean-reversion `pull` toward the range's midpoint (keeps it roaming the whole plausible range instead of wandering off and sitting at one edge forever), small per-tick `jitter`, an occasional larger "event" kick (~1/75 chance per 200ms tick, roughly once per 15s per metric — a real value doesn't drift in perfectly uniform steps; something like a throttle change or a bump shows up as a bigger jump), and velocity that DECAYS (`*0.93`) rather than resetting each tick — that decay is what makes movement read as slow momentum/inertia rather than tick-to-tick jitter, since one random nudge keeps influencing several following ticks' direction.
**A real dimensional-analysis bug, found and fixed on first test**: the first version computed `pull` as a dimensionless fraction (`(mid-value)/range * 0.002`, roughly 0-0.002) but `jitter` in actual value units (`(rand-0.5) * range * 0.003`) — adding these together made the restoring pull comically weak relative to jitter (off by roughly a factor of `range`), and the walk visibly got stuck pinned at exactly a clamped edge (e.g. speed sitting at exactly `0` for 5+ consecutive ticks) — read as robotic, exactly the opposite of "closer to real data." Fixed by keeping every quantity (`pull`/`jitter`/`velocity`/`value`) in the same actual-value units throughout (`pull = (mid - value) * 0.004`, a proper proportional spring force) — verified directly by sampling `testWalkState` values across 40 ticks post-fix: smooth, gradually-reversing drift with no edge-pinning and no periodicity.

### `MAX_POINTS` is viewport-dependent, decided once at load — not the same on mobile and desktop
Checked once via `matchMedia('(max-width:860px)')` when the script first runs (60 on mobile vs. 150 on desktop, both still ~the same *time* window at the 5Hz cadence — just fewer points per pixel on a narrower chart). Not re-evaluated on resize/rotation — the `history`/`historyTimes` arrays are pre-sized to this value once (`initHistory`-equivalent code near `STATE / HISTORY`), and there's no live-resize path for them. If a metric/chart needs a different point-density rule later, this is the one place to change it.

### Live charts DO now animate on update — `chart.update()`, not `chart.update('none')`
This reverses an earlier explicit choice ("animation stays OFF for live data updates... looks laggy, not smooth"). `LIVE_ANIMATION` (`{duration:250, easing:'easeOutQuad'}`) is short enough to resolve before the next 200ms tick lands, which is what makes it read as a smooth "line growing" motion instead of the earlier hard per-tick cut, or (if the duration were left longer) visible tween-stacking lag. If update cadence or `LIVE_ANIMATION.duration` ever change together, keep duration comfortably under the tick interval.

### Haptics on chart scrub are attached post-construction, not baked into `BASE_OPTS`
`attachHaptics(opts)` sets `opts.onHover` after `safeChart()`/the multi-chart build their options via `JSON.parse(JSON.stringify(BASE_OPTS))` — functions don't survive a JSON round-trip, so putting `onHover` directly on `BASE_OPTS` would silently vanish on every chart built that way. `navigator.vibrate` is Android-only (silently absent, not an error, on iOS/desktop) and fires once per newly-crossed data-point index while scrubbing, not per pointer-move frame.

### `contextmenu` is blocked at the document level for non-data elements too — CSS alone wasn't enough
A real gap found after the `user-select`/`-webkit-touch-callout` CSS fix (see below) shipped: Android Chrome's "Smart Text Selection" long-press card (the "Search/Share/Copy" popup with its own haptic buzz — colloquially "the Google thing") can still fire on plain non-selectable text regardless of either CSS property, because Chrome forwards long-press coordinates to Android's system text classifier as part of the same gesture pipeline that also drives the desktop `contextmenu` event — it isn't gated by CSS selection state at all. `document.addEventListener('contextmenu', ...)` (near the top of `<script>`, `passive:false` since a passive listener can't call `preventDefault`) calls `preventDefault()` unless the target is inside `COPYABLE_SELECTOR` (the same element list the CSS re-enables selection on, kept as one shared JS constant). This reliably suppresses it everywhere CSS alone didn't, while leaving actual data values (stat readouts, raw log, etc.) untouched.

### Nothing is selectable/copyable except actual telemetry values
`body { user-select:none }` by default, with `user-select:text` re-enabled only on `.stat-value`/`.dedicated-value`/`.stat-unit`/`.dedicated-unit`/`#raw-log`/`#sessions-table td` (and `#esc-mode`/`#esc-state`, which are also `.dedicated-value`). Adding a new numeric/string readout element outside those classes means it won't be copyable unless it's given one of them (or added explicitly) — this was deliberate, not an oversight, if a new display element ever seems to need long-press-to-copy and doesn't have it.
**`-webkit-touch-callout` has to be set alongside `user-select`, not instead of it** — a real gotcha found after the above shipped: iOS Safari's long-press "Copy/Look Up/Share" callout menu is gated by `-webkit-touch-callout`, a *separate* property from `user-select` specifically on iOS — `user-select:none` alone stops the text from visibly highlighting, but iOS still pops that callout menu on a long-press regardless, since the menu there isn't gated by selection state at all. `body` sets `-webkit-touch-callout:none` alongside `user-select:none`, and the same selector list that re-enables `user-select:text` also sets `-webkit-touch-callout:default`. If a future new copyable element is added, it needs both properties re-enabled together, not just `user-select`.

### Mobile dedicated-value pages: even card count → all landscape-rectangle tiles (2/row), odd → last one is a full-width rectangle
Pure CSS, no JS/count-tracking: `.big-row .dedicated-big-card:nth-child(odd):last-child` only matches when the last card's position is itself odd, which is only possible when the row's total count is odd. Pages with a single card (Speed, Temperature) don't use `.big-row` at all and are untouched by this — already a full-width rectangle by default, same as before. Tiles are `aspect-ratio:8/5` (a slight landscape rectangle), not `1/1` — a first pass used true squares, but that took more vertical scroll height than needed; 8/5 keeps the 2-per-row grid while shrinking each tile's height.

### Live charts use a real numeric time x-axis with fixed {x,y} points — NOT Chart.js per-update animation
Two rounds of chasing "the line bugs out"/"graphs don't show up in Test Mode" both traced back to the same earlier mistake: making live charts animate on every `update()` call (`LIVE_ANIMATION`, `chart.update()` instead of `'none'`). That approach is fundamentally wrong for a shifting window of data — Chart.js's animation tweens each ARRAY INDEX's value old→new, but `pushHistory()` shifts the oldest sample out and pushes a new one in every tick, so index 0's "old" value and "new" value belong to two different samples entirely. Every point ends up rippling toward a neighbor's value instead of sliding — the actual cause of "bugs out, isn't smooth." It also caused a second bug: `chart.resize(w,h)` silently no-ops (defers itself) whenever that chart has a Chart.js animation in flight, and a chart fed continuous updates can have "an animation running" almost permanently — so the tab-switch resize fix got silently deferred forever, and a newly-active chart's canvas stayed frozen at its original (often 0×0) size for as long as updates kept flowing. Confirmed directly by watching `canvas.width` stay 0 through repeated `chart.resize()` calls while Test Mode's updates kept streaming, until `chart.stop()` (cancels the in-flight animation) was called first.

The real, correct fix is architectural: `BASE_OPTS.scales.x` is `type:'linear'` (real epoch-ms values, not category/string labels), and every chart point is a genuine `{x: epoch-ms, y: value}` pair (`safeUpdateChart(chart, times, values)` zips `historyTimes`/`history[k]` into these; the multi-chart's datasets in `applyHistory()` do the same). A point's (x,y) never changes after it's first recorded — confirmed by watching one sample's exact `{x,y}` stay bit-for-bit identical across ticks while `scales.x.min`/`max` advanced. Because of that, every still-visible point simply lands at a slightly different pixel position each redraw (its fixed timestamp falling in a different spot within the advancing `[min,max]` window) — which alone reads as continuous smooth scrolling at 5 redraws/second, the same way a heart-rate monitor trace looks fluid from frequent redraws with no animation system involved. `animation:false` again (no `LIVE_ANIMATION`), `chart.update('none')` always — there's nothing left for Chart.js's animator to usefully tween, and the whole resize-deferral trap is moot as a result. `attachTimeAxis(opts)` (assigns the x-tick formatter and tooltip title formatter — both functions, so like `attachHaptics()` this has to run *after* `JSON.parse(JSON.stringify(BASE_OPTS))`, not live on `BASE_OPTS` itself) needs to be called for any future chart built off `BASE_OPTS`, same as `attachHaptics()`. `forceChartResize()` (`stop()` then `resize()`) is kept as a defensive no-cost guard against this trap ever recurring, not because it's load-bearing today.

### Live chart history is a single GROWING buffer with a global time-RANGE toggle — not a fixed-size ring anymore
`history[k]`/`historyTimes` used to be a fixed-size ring (`MAX_POINTS`, shift-then-push, capped at ~30s/12s desktop/mobile). Replaced with a growing buffer per metric, retained until `HISTORY_HARD_CAP_MS` (4h) regardless of what's currently selected for display, plus a separate global `currentRangeMs`/`currentRangeKey` (`RANGE_OPTIONS`: 1/5/15/30 min or `null`/`'all'` for "All") that's just a VIEW into a slice of that buffer — switching ranges never re-fetches or discards data, it only changes which slice `applyHistory()` hands to Chart.js.
**UI is a row of buttons per page, not one dropdown** — `renderRangeToggles()` renders identical `.range-toggle-btn` markup into every `[data-range-slot]` container (one above the charts on each of the 8 dedicated pages, one inside Overview's Customize panel — Overview's own mini-charts don't get a visible row of their own, folded into Customize instead since that's already where Overview's other chart-display settings live), all reflecting/controlling the SAME shared state via one delegated `document` click listener. An earlier version used a single `<select>` in the header — replaced because the user wanted the control visible above the graphs on each page instead. If a page renders charts and doesn't have a `[data-range-slot]` in its markup, its charts still obey the global range correctly, they just have no on-page way to change it from that page.
**"All" means "the current continuous run", not the full DB-backed session.** This is a deliberate scope line matching this file's existing documented split between the client-side-only live charts and the separate, durable, server-side Sessions/CSV-export feature (see the Sessions rules elsewhere in this file) — a session's full pre-page-load history is what the Sessions tab already exists for; merging that fetch into the live charts here was a deliberately avoided scope expansion, not an oversight. "All" is still bounded by `HISTORY_HARD_CAP_MS`, same as every other range, and further scoped by the stale-gap-cutting rule below.
`findTimeIndex()` (binary search on the always-ascending `historyTimes`) finds the window's start index in O(log n) rather than a linear scan — matters because the buffer can hold tens of thousands of entries at the long-range end. `chartIsVisible()` (checks `canvas.offsetParent !== null`) gates which charts `applyHistory()` actually rebuilds each tick — rebuilding a data array that can now be 1,500-9,000+ points for every one of ~25 charts every 200ms, whether visible or not, was the exact kind of cost already fixed once this session (see the Test Mode paint-starvation rule elsewhere in this file) and would reintroduce it at this new, larger scale. A chart naturally catches up within one poll tick of becoming visible (`applyHistory()` re-runs on every new live packet regardless of the active tab) — EXCEPT when nothing is live (offline, or Test Mode off), which is why the tab-click handler explicitly calls `applyHistory()` itself right after the existing `forceChartResize()` calls, not just relying on the next packet that might never come.
`safeUpdateChart(chart, times, values, xMin, xMax)` takes explicit x-axis bounds now (used to derive them from `times[0]`/`times[last]`) — the selected window can be much wider than however much real data has actually arrived, and the axis needs to show the FULL currently-effective window immediately (see the grow/gap-cutting rule below for how that window is computed), not shrink-wrap to whatever data exists so far.
Decimation (Chart.js's built-in plugin for exactly this "many points, narrow chart" scenario) was deliberately NOT added — it looked like the right tool for the 15/30-min-range point-density concern, but requires `parsing:false` and specific data-shape assumptions this environment couldn't visually verify render correctly, so it was left out rather than risk an unverifiable rendering bug. Worth reconsidering if long-range charts turn out to look/perform poorly on a real device.

### A live chart's window GROWS from a 1-minute floor up to the selected range, and RESETS on a stale gap — `activeRunStart`/`CHART_GAP_STALE_MS`
Two asks that turned out to be the same underlying mechanism: (1) a chart shouldn't jump straight to a mostly-empty full-range window the instant a run starts — it should start small and grow into the selected range as real time actually elapses; (2) a >5s gap in live data (LoRa dropout, agent restart, car parked) shouldn't have Chart.js draw a straight line bridging the dead time as if it were a continuous reading. Both are handled by one pair of variables tracked in `pushHistory()`: `lastPacketTime` (the previous real packet's timestamp) and `activeRunStart` (when the CURRENT uninterrupted run began). If a new packet arrives more than `CHART_GAP_STALE_MS` (5000ms) after `lastPacketTime`, the entire retained buffer is wiped and `activeRunStart` resets to that new packet — no attempt to render a visual "gap" mid-chart, the chart just re-anchors cleanly to the new run, discarding the disconnected old one (this applies even in "All" mode — see its own rule above).
`applyHistory()` computes the actual displayed window from this: `effectiveSpan = max(RANGE_FLOOR_MS, min(now - activeRunStart, currentRangeMs))` (or, for "All", `max(RANGE_FLOOR_MS, now - activeRunStart)` with no upper term). `RANGE_FLOOR_MS` (60000, 1 minute) is the floor mentioned above — even the instant the very first packet of a fresh run arrives, the chart shows a 1-minute-wide axis, not a razor-thin one that stretches wider with every single point. As real time elapses past that floor, the window grows 1:1 with elapsed time until it reaches `currentRangeMs`, then holds/slides at that fixed width like before. Verified directly: faking `activeRunStart` to different ages produces exactly 60000ms (floor, <1min elapsed), 120000ms (mid-growth, 2min elapsed), and 300000ms (capped at a 5min selection, 20min elapsed) window spans; and a manually-pushed packet timestamped >5s after the last one drops the buffer from 104 points to 1 while a <5s gap leaves it untouched.

### Session charts (`viewSession()`) intentionally were NOT converted to the {x,y}/linear-axis pattern
They're one-shot static renders built once from a session's full history, not incrementally updated — the "index reassignment" problem above only exists for a *shifting* window, which a static array never has. They still use label-array/category-axis Chart.js config; no need to touch them if the live-chart x-axis approach changes again.

### Download Agent button downloads a self-hosted, PRE-BUILT `public/GreenpowerAgentSetup.msi` — behind a confirmation modal
Went through four forms: `download-directory.github.io` (third-party page, no auto-close, left a "Downloaded 10 files! Done!" status screen visible every time) → a self-hosted `greenpower-agent.zip` (same-origin, native save prompt, zero intermediate page) → a real Inno Setup `.exe` installer (worked, but got hard-blocked on Intune-managed devices by a Defender ASR rule — see the RESOLVED note and historical section further down for the full investigation) → **a real WiX `.msi` installer**, `public/GreenpowerAgentSetup.msi`, built from `receiver_agent/installer/GreenpowerAgent.wxs` (see `receiver_agent/CLAUDE.md` for the installer's own rules — no-admin-required, Node.js-still-a-prerequisite, the WixQuietExec fix for a real visible-cmd-window bug, etc.). Confirmed by the user actually installing it successfully on the same machine the `.exe` was blocked on — not just "should work."
Because this is a real installer someone would run (not an inert zip of source files), `#download-agent-btn` is a `<button>`, not a plain `<a href download>` — clicking it opens `#download-confirm-modal` (a simple centered backdrop+panel, desktop-only since the button itself already is) explaining what it downloads and why, with a Cancel button and a real `#download-confirm-go` `<a href="/GreenpowerAgentSetup.msi" download>` that both fires the native save prompt AND closes the modal on click. Same closed-state pattern as `.mobile-navsheet` (`opacity:0`/`pointer-events:none`, never `display:none`) for the same reason — see that rule elsewhere in this file.
This is a **snapshot, not a live build** — it does NOT regenerate itself when `receiver_agent/` changes, and there is no build step or server route wired up to keep it in sync. **Whenever any file in `receiver_agent/` changes, this installer must be rebuilt by hand and re-copied** (or the downloaded agent silently goes stale) — from `receiver_agent/installer/` (no admin needed, using the portable WiX v3.14.1 binaries — see `receiver_agent/CLAUDE.md` for exactly how those were obtained without needing WiX's own admin-requiring installer):
```
candle.exe -arch x64 -ext WixUtilExtension GreenpowerAgent.wxs
light.exe -ext WixUIExtension -ext WixUtilExtension GreenpowerAgent.wixobj -o dist\GreenpowerAgentSetup.msi
```
then copy `installer/dist/GreenpowerAgentSetup.msi` over `telemetry_web/public/GreenpowerAgentSetup.msi`.
**Not run/tested as a real install in the session that built it** — compiling `.iss` → `.exe` was verified (file exists, correct size, serves with the right content-type/length from `server.js`), but actually double-clicking the resulting installer would create a real persistent Windows Startup entry, run `npm install`, and start the agent talking to the live telemetry API — real, only-somewhat-reversible side effects that weren't triggered without asking first. Worth an actual install test before trusting this fully.

### ✅ RESOLVED — switched from the Inno Setup `.exe` to a WiX `.msi`; the ASR block below no longer applies
Everything in this section and the next describes the `.exe`-era investigation, kept intact because the reasoning is exactly why the `.msi` was tried next — but **the distributed installer is `public/GreenpowerAgentSetup.msi` now, not a `.exe`.** Confirmed empirically, not just reasoned: `Get-AuthenticodeSignature`/`OpenRead` on the freshly-built, zero-reputation `.msi` completed cleanly (no `UnauthorizedAccessException`), unlike the `.exe`, which was blocked even for a plain file read. The ASR rule's own documented scope (`.exe`/`.dll`/`.scr`) doesn't cover MSI packages — `msiexec.exe`, not the `.msi` file itself, does the actual installing, and `msiexec.exe` is already a trusted system binary. The user then ran the real install themselves and **confirmed it completes successfully** on the same Intune-managed machine the `.exe` was blocked on. See `receiver_agent/CLAUDE.md` for the WiX/MSI-specific build details (`GreenpowerAgent.wxs`, the WixQuietExec fix for a real "visible cmd window during install" bug, etc.) — this file's own Download Agent button rule (further up) documents the current `.msi`-based state; treat everything below as historical context for WHY, not the current distribution mechanism.

### ⚠️ (Historical — see RESOLVED note above) The Inno Setup `.exe` was UNSIGNED and got blocked on Intune-managed/WDAC-enforced devices — reproduced firsthand, not theoretical
The user tried the real download on their own machine and hit `"Windows cannot access the specified device, path, or file. You may not have the appropriate permissions to access the item."` when running it. Diagnosed directly (`dsregcmd /status`, `Get-AuthenticodeSignature`, `citool.exe --list-policies`, `icacls`): the machine is Azure-AD/Intune-enrolled, and `Get-AuthenticodeSignature` itself got `UnauthorizedAccessException` on the file despite normal-looking ACLs (Full Control listed for the owning account) — the signature of Windows Defender Application Control (WDAC) blocking an unrecognized/unsigned binary at the OS level, not an actual filesystem permission problem. Smart App Control was confirmed OFF (`VerifiedAndReputablePolicyState:0`) at the time, so it's specifically WDAC/org policy via Intune, not SAC.
**This isn't one machine's quirk — it's structural.** Any similarly-managed device (very plausibly most/all of this team's org-issued laptops, if they're on the same tenant policy) will hit the same block. The user was told this explicitly and chose to **leave the installer live anyway** rather than revert to the old `greenpower-agent.zip` fallback — a deliberate, informed call, not an oversight. Don't "fix" this by silently reverting to the zip; that decision was already made.
**Self-signing was tried and empirically ruled out TWICE, not just assumed unhelpful.** Generated a self-signed code-signing cert (`New-SelfSignedCertificate`) and attempted to sign the installer with `signtool.exe` (obtained standalone via the `Microsoft.Windows.SDK.BuildTools` NuGet package — much lighter than installing the full Windows SDK, no admin needed) — **`signtool` itself got blocked before it could touch the file**, with the exact same "Access is denied" symptom, in a scripted/headless attempt. The Windows event log (`Microsoft-Windows-Windows Defender/Operational`, event ID 1121) named the precise cause: **ASR rule `01443614-cd74-433a-b99e-2ecdc07bfc25`**, *"Block executable files from running unless they meet a prevalence, age, or trusted list criteria."* That rule name is the important part — it's reputation-based (built up over time/download volume in Microsoft's cloud intelligence), not simply "is this file signed." A self-signed cert has zero reputation, identical to an unsigned file, so it would not have helped even if signing itself hadn't also been blocked.
The user then independently ran the exact same `New-SelfSignedCertificate`/`signtool sign` commands themselves, interactively, in their own visible PowerShell window — **identical "Access is denied" failure.** This rules out the one remaining theory (that a scripted/headless attempt might simply be missing an interactive override prompt a human would see and could click through) — there is no such prompt for this rule in this configuration; it's a hard block with zero user-facing override, enforced the same way regardless of who or how it's invoked. Don't re-attempt self-signing as a workaround without new information (e.g., the org's ASR policy actually changing) — this has been conclusively tested twice, not just reasoned about.
This also means **a freshly-issued real Azure Artifact Signing certificate might not immediately clear this specific rule either** — signing helps most against SmartScreen and general trust, but this particular ASR rule is stricter than that; a brand-new signed file may still need to build up download reputation before this rule stops flagging it. All private key/cert material generated for these tests was deleted after each finding — nothing was left lying around unused.
**The real fix is code-signing** (researched, not yet started, and per the above, not guaranteed to fully resolve this specific ASR rule on its own): Microsoft's **Azure Artifact Signing** (rebranded from "Trusted Signing" in 2026, ~$9.99/mo Basic tier) is the realistic path given this org is already on Azure AD — but it requires a live, webcam-based government-photo-ID identity verification (via AU10TIX) tied to one specific real person (not the team/org — a passport/driver's license/state ID, explicitly not a student ID), 1-20 business days of Microsoft processing after that, and an Azure subscription with an Individual-type billing account (or a full Organization identity validation instead, if there's a registered legal business entity — more paperwork, not obviously easier). None of this can be completed by an AI assistant — it needs a specific human's real ID and live phone. If this gets set up later, wiring the resulting cert into the build is the easy part: Inno Setup's `[Setup]` section supports `SignTool=` directly.
**The likely faster real fix: ask the org's IT to add an ASR exclusion** for this specific file/app — sidesteps identity verification, cost, and reputation-building entirely, since it's a policy exception rather than a trust mechanism. This needs the org's actual IT/Intune administrator, not something achievable from a single machine.

### Chart tooltips disappear immediately when a touch is released — `attachTouchTooltipAutoHide()`
Chart.js's default `events` list includes `touchstart`/`touchmove` (so scrubbing a finger across a chart shows/updates the tooltip) but **not** `touchend` — there's no touch equivalent of a desktop `mouseout` to clear it, so on a touch device the tooltip used to just stay pinned at wherever the finger last was, indefinitely, until the next tap started a new one. `attachTouchTooltipAutoHide(chart)` adds real `touchend`/`touchcancel` listeners on `chart.canvas` that clear it instantly (`chart.setActiveElements([])` + `chart.tooltip.setActiveElements([], {x:0,y:0})` + `chart.update('none')` — the standard documented Chart.js pattern for programmatically hiding a tooltip, `'none'` mode for no fade). **An earlier version faded it out after a 1s delay instead** — the user tried that behavior and asked for an immediate disappearance in its place, so if a delay/fade ever seems worth reintroducing, confirm that's actually wanted first, it was a deliberate choice already made once here.
Unlike `attachHaptics()`/`attachTimeAxis()` (which patch an **options object** before `new Chart(...)` is called, since those get `JSON.parse(JSON.stringify(BASE_OPTS))`-cloned per chart), this one takes the **chart instance itself** and must run *after* construction (`attachTouchTooltipAutoHide(new Chart(...))`) — it needs `chart.canvas`/`chart.update()`, which don't exist until the chart is actually built. Wired into all three chart-construction sites: `safeChart()` (ov/big charts), the multi-chart, and each `viewSession()` session chart. `TOOLTIP_OPTS.animation` (`{duration:250}`) still governs the tooltip's own show/appear transition (unaffected by this — only the hide path uses `'none'`), separate from the chart-level `animation:false` used everywhere else in this file.

### Mobile nav sheet's CLOSE animation was silently skipped — `display:none` doesn't wait for a transition
Real bug, not a duration/easing issue: `.mobile-navsheet`/`.mobile-navsheet-backdrop` used to be in the same "hidden by default" `display:none` selector as the rest of the mobile-only UI, with `.show` explicitly setting `display:block`. Opening worked fine (transitioning from a `display:none` starting point into visible is a normal, supported browser pattern) — but *closing* did not: removing `.show` reverted to the base rule's unconditional `display:none`, which isn't a transitionable property and takes effect the instant the class changes, yanking the sheet out of rendering before its opacity/transform transition got even one frame to play. It just vanished instead of animating out. Fixed by pulling both elements out of that shared `display:none` selector entirely and relying purely on `opacity:0`/`pointer-events:none` for the closed state (verified structurally both directions using this project's documented transition-disable trick — see the tooltip rule near the bottom of this file). Costs nothing extra on desktop: both are `position:fixed`, already out of normal layout flow, so being "always rendered but invisible" has no layout effect there. If a similar show/hide-via-`.show`-plus-`display:none`-base-rule pattern is used for a future element that needs a close animation, this is the same trap — don't reach for `display:none` as the closed state if a close transition matters.

### `.chart-hide-btn`'s icon is centered with flex, not text-align/line-height
`text-align:center` + `line-height:<button-height>px` only centers on a text baseline — it doesn't reliably vertically-center a differently-sized inline-flex child (the `.icon` span, 10×10 inside an 18×18 circle), and visibly lands off-center depending on the icon's own metrics. `display:flex; align-items:center; justify-content:center` on the button centers by actual box geometry instead, regardless of icon size — the fix, if a similar tiny-icon-in-a-circle-button pattern is added elsewhere.

### `color-scheme: dark` (meta tag + CSS) — separate from `theme-color`, needed for OS-drawn chrome (status/nav bar)
`theme-color` only tints the in-browser tab/address-bar strip. The OS-level chrome around an *installed* PWA (Android's status bar AND bottom gesture/nav bar) follows the page's declared `color-scheme` in current Chrome, not `theme-color` — without it, that chrome can default to the system light/dark setting regardless of how dark the page itself is. Declared both as `<meta name="color-scheme" content="dark">` and `:root{color-scheme:dark}` (belt-and-suspenders — different engines/contexts read one or the other). Still won't retroactively fix an *already-installed* copy of the app without an uninstall+reinstall (manifest/meta values are cached at install time) — see SESSION_NOTES.md.

### Mobile edges are tinted to match Android's *actual* system-bar colors — the opposite approach from `theme-color`/`color-scheme`
After confirming (via the Chromium issue tracker, a matching bug report against another unrelated PWA, and MDN's own docs never actually claiming navigation-bar control in the first place) that `theme-color`/`color-scheme` genuinely can't reliably make Android's system bars match the page — this flips the direction: match the page's edges to what Android is *actually* painting instead. Two DIFFERENT measured values, not one — the status bar (top) and gesture/nav bar (bottom) are not the same color on this device: **`#0b0c11`** (status bar — nearly identical to `--bg0` already, a 1-unit difference) and **`#171719`** (nav bar — a real, visible difference from `--bg0`). An earlier pass used a single `#5f5f5f` for both; that measurement was wrong/stale and has been fully replaced — if you see `#5f5f5f` referenced anywhere, that's stale. Both are user-measured constants sampled directly off the real installed app, not derived from anything else — if Android's actual bar colors ever shift (device/OS update), these need resampling, not a formula fix.

Scoped to mobile only, in four places:
1. `body { background:#0b0c11; }` inside `@media(max-width:860px)` — the status-bar value. `#app` carries its own explicit `background:var(--bg0)` on desktop (added specifically for this; it had none before, relying on `body` showing through), so `body`'s mobile color is only ever visible in the genuine top edge gap (`safe-area-inset-top` strip above `#header`) — **never** the bottom, since `#app`'s box already extends the full `100dvh` down to the physical bottom edge, leaving no gap there for `body` to show through at all.
2. `#app { background:linear-gradient(to bottom, #0b0c11, #171719 80%); }` (mobile-only override, replacing the flat `var(--bg0)`) — this is what actually carries the bottom-edge (nav-bar) fix, since `body` structurally can't reach there (see above). A full-height gradient, not a separate bottom-only strip: both endpoints are close near-black tones, so it reads as "a background" across the whole scrollable dashboard, not a visible seam partway down the page — and `--panel` (the card overlay, a barely-there 3.5% white tint tuned for a near-black base) still reads fine against either end, unlike the earlier flat-`#5f5f5f`-everywhere attempt would have.
   The second stop is pinned at **80%, not 100%**. A plain 2-stop gradient (`A, B`) only reaches the *exact* second color at the very last pixel row — measured directly: the nav bar pill sits at roughly 92-99% down a typical phone viewport, where a 100%-terminated gradient was still interpolating to something like `#161619` instead of the true `#171719` — a real, if small, off-by-one-unit mismatch, not the exact measured color. Per the CSS gradient spec, once a gradient passes its *last* explicit color stop, the color holds exactly constant for the rest of the box — no further interpolation — so pinning the stop at 80% guarantees the entire nav bar/sheet zone (with comfortable margin for a shorter device or more safe-area inset pushing the pill up further) sits on the *exact* measured `#171719`, not an approximation of it.
3. `#header { background:#0b0c11; }` (mobile-only override, flat and opaque — NOT the desktop `rgba(13,16,23,.85)`). Two real problems with reusing the desktop translucent tint here: it has a visible blue cast (blue channel 23 is the highest of the three; `#0b0c11`'s channels are much closer to neutral), and being translucent it blends with whatever's scrolling behind it rather than staying one fixed color — both read as a mismatch against the real, neutral, opaque status bar directly above it. Flat+opaque fixes both.
4. A second, `media="(max-width: 860px)"`-gated `<meta name="theme-color">` tag (`#0b0c11`) alongside the existing unconditional one (`#0a0c11`) — same mechanism browsers already use for `prefers-color-scheme` light/dark variants, so desktop's tab/toolbar tint is completely untouched. A single flat meta value can't represent the top/bottom split the way `#app`'s gradient does — this only ever represents the top.

`manifest.json`'s `background_color`/`theme_color` are `#0b0c11` (top/status-bar value — these only ever apply to the mobile PWA install/splash-screen context, so no mobile-only gating needed, and a splash screen is a single flat color with no gradient concept anyway).

### Scrollable+rounded elements use `overflow: hidden auto` (or `auto hidden`), never a bare `overflow-y:auto`/`overflow-x:auto`
`#raw-log`, `#sessions-table-wrap`, `.mobile-navsheet` all combine `border-radius` with scrolling. A bare `overflow-y:auto` leaves `overflow-x` at its default `visible` — but per the CSS Overflow spec, a box can't have one axis `visible` and the other not, so the un-set axis gets silently computed to `auto` too. That implicit second scrollbar axis was what let the (custom-styled, `::-webkit-scrollbar-*`) scrollbar draw outside the element's own border-radius curve on some browsers/devices — the user's literal report was seeing it "go past rounded edges." Explicitly setting the axis that's genuinely never wanted to `hidden` (not just leaving it unset) avoids the dual-auto computation instead of fighting it after the fact. `#sessions-table-wrap` scrolls horizontally (`overflow:auto hidden`) while the other two scroll vertically (`overflow:hidden auto`) — match the direction to whichever axis that specific element actually uses.

### `.mobile-navbar`'s drop shadow is symmetric (zero offset), not upward-biased and not a plain downward one
Went through four states this session: a plain downward shadow (`0 10px 30px -10px rgba(0,0,0,.6)` — cast a dark halo/gradient into the ~10px+ gap toward the real Android nav bar below it, reading as a color mismatch even once the actual background colors, see `#app`'s gradient rule above, were correct) → removed entirely (the safe fallback while nothing else was resolved) → upward-biased (`0 -2px 8px rgba(0,0,0,.3)`, negative y-offset, once the user asked for the pill to have real depth back) → **`0 0 6px rgba(0,0,0,.3)`, equal on every side**, per an explicit follow-up request for symmetric depth rather than a lopsided lift. With zero offset, the shadow's downward reach past the element's own bottom edge is the FULL blur radius (6px here, not `blur − |offset|` the way the upward-biased version's was) — kept deliberately small so that full reach still stays inside the ~10px+ measured gap; a bigger blur here would need an offset again to stay clear, they trade off against each other. Since `.mobile-navbar` only ever renders on mobile (base rule is `display:none`, only turned on inside `@media(max-width:860px)`), this lives on its unconditional base rule rather than needing a mobile-only override. If this pill's height/position or the gap to the real nav bar ever changes meaningfully, re-verify that 6px reach still clears it (see the `.mobile-navsheet` shadow rule below for how to actually measure that gap, not just assume it from the raw CSS `bottom:` values).

### `.mobile-navsheet-backdrop`'s dimming fades out before it reaches the nav bar — the real source of the "big black shadow", not the sheet's own box-shadow
After tightening the sheet's own `box-shadow` (below), the user reported the shadow reaching the nav bar was still there — the real culprit was different: `.mobile-navsheet-backdrop` (the full-screen dimming overlay behind an open sheet, `position:fixed; inset:0`) covers the *entire* viewport, and while the nav bar pill itself sits above it in z-index (`.mobile-navbar` is z-index 15, backdrop is 14 — the pill's own opaque pixels are genuinely untouched), the small gaps/margins/rounded corners *around* the pill are still part of "the page behind the sheet" and were getting darkened by that same flat black — reading as a big shadow reaching the nav bar, distinct from (and much larger than) the sheet's own small box-shadow. Fixed by making the backdrop's `background` a gradient instead of a flat color: full dimming holds through 70% down the viewport (still dims the actual page content behind the sheet, the whole point of a backdrop), then fades smoothly to fully transparent by 84% — the nav bar pill's own top edge measures ~91.6% down a typical phone (same measurement used for the `#app` gradient fix above), leaving a real ~7-8% margin, not just enough to technically clear it. If the nav bar's position or height ever changes, re-check that 84% still lands comfortably before its new top edge.
**Also fades in from transparent over the first 14%, mirroring the 70%→84% fade-out span, per a follow-up "equal on top and bottom" request** — the original version was solid black from the very top edge (0%) and only faded at the bottom, which read as lopsided once actually compared: a "shadow" that only softened on one side. The now-4-stop gradient (`0% transparent → 14% full → 70% full → 84% transparent`) gives both edges the identical 14-percentage-point fade shape, holding solid only through the middle ~56% of the viewport.

### EVERY full-screen dimming backdrop on mobile uses this same symmetric fade-in/fade-out gradient now, not just the nav sheet's
Per explicit request ("any shadow that drops on the entire screen on mobile... equal on the top and bottom"), the same 4-stop gradient shape validated above for `.mobile-navsheet-backdrop` (`0% transparent → 14% full → 70% full → 84% transparent`) is now also applied, mobile-only, to `.confirm-modal-backdrop` (covers the download-agent/exit-app/clear-data confirmations — exit-app in particular is Android-back-gesture-triggered, so this is a real mobile case, not just theoretical) and `.update-banner-backdrop` (shown on both platforms; only the mobile override changes, desktop keeps its base flat fill). Each keeps its OWN base color/opacity (`rgba(0,0,0,.55)` for confirm-modal, `rgba(5,6,10,.72)` for the update banner) — only the gradient SHAPE is shared, not the color itself. These mobile overrides live in the file's later `@media(max-width:860px)` block (search for "Any full-screen dimming backdrop needs the SAME"), positioned AFTER each selector's own unconditional base rule — same real cascade-order trap documented in the onboarding-overlay rule below (an override placed BEFORE its base rule's position in the source loses to it regardless of media query, even though it "looks" more specific). If a future full-screen `position:fixed;inset:0` dimming layer is added for mobile, give it this same treatment rather than a flat fill, and double-check its override actually lands after its base rule.

### `.mobile-navsheet`'s drop shadow was removed entirely (not just tightened)
Same root problem as `.mobile-navbar` above, one step removed: the sheet floats *above* the nav bar pill, and its original shadow (`0 18px 44px -14px rgba(0,0,0,.65)`) reached far enough down to still visibly darken the gap toward the pill below, perturbing the same precisely-tuned background. A first attempt tightened it to `0 8px 20px -6px rgba(0,0,0,.5)` (~28px reach) reasoning the sheet-to-pill gap was ~78px — but the ACTUAL measured gap (sheet's real bottom edge to the pill's real top edge) is only **~4-10px**, since `bottom:calc(78px+safe-area-inset-bottom)` on the sheet and `bottom:10px+safe-area-inset-bottom` + 58px height on the pill nearly cancel out. No shadow size can both read as a real depth cue and clear a gap that small, so this went to `box-shadow:none` entirely — same call already made for `.mobile-navbar`, same reasoning, just a step later. If the sheet's `bottom` offset or the pill's height/position ever change enough to meaningfully widen this gap, a real shadow could be reconsidered — but always measure the ACTUAL rendered gap first (`getBoundingClientRect()` on both, not the CSS `bottom:` values alone), not just assumed from the raw CSS numbers.

### `display: "standalone"` (not `"fullscreen"`) — tried and deliberately reverted
Even with `theme_color`/`background_color`/`color-scheme` all correctly matching `--bg0`, the installed-PWA (WebAPK) navigation bar still didn't reliably pick up that color — a real, documented Chromium/Android platform limitation: full, reliable system-bar-color control only exists for a proper Trusted Web Activity (a native Android wrapper app, not a plain "Add to Home Screen" WebAPK). `display_override:["fullscreen","standalone"]` was tried as a workaround (hides the OS status/nav bar entirely instead of trying to color-match it) but the user found fullscreen mode itself buggy on their device and asked to revert — so the color mismatch stands as a known, currently-unresolved limitation of the plain-WebAPK install path, not something to re-attempt via `display_override` without the user explicitly asking again. If this needs revisiting, the only remaining real lever is a proper Trusted Web Activity wrapper (Bubblewrap or similar) with its own `navigationColor` field — a materially bigger undertaking (native build/signing, likely Play Store or sideload distribution) than anything achievable purely from `manifest.json`/CSS.

### Mobile nav (toolbar + sheet) is fully user-customizable — rendered by JS from Settings, not static HTML
Multi/Sessions/Raw were briefly removed from mobile nav entirely, then explicitly asked back — instead of just re-adding static markup, mobile nav became genuinely customizable per a follow-up request: reorderable AND individually hideable, from a new Settings → "Mobile Toolbar & Pages" list. This is a real structural change, not a CSS tweak:
**Data model** — `MOBILE_NAV_ITEMS` (12 entries: every page except Settings, which is deliberately excluded from this whole system — see below) defines the canonical key/label/icon set and, via its own array order, the DEFAULT `mobileNavOrder` (first 5 = toolbar, matching the original static layout exactly: overview/speed/power/rpm/esc, then temp/imu/raceline/link/multi/sessions/raw). `mobileNavOrder` (`gp_mobile_nav_order`) is the user's full custom ordering of all 12 keys; `mobileNavHidden` (`gp_mobile_nav_hidden`) is which keys are excluded from mobile nav entirely. `visibleMobileNavOrder()` = `mobileNavOrder` minus hidden keys; the **toolbar is always just `.slice(0, MOBILE_NAV_TOOLBAR_SLOTS)`** (5) of that visible list, and the **sheet is everything after it** — there's no separate "which slot" concept beyond position in one ordered list, which is what makes "move X into the toolbar" as simple as reordering past index 4.
**Rendering** — `renderMobileNav()` rebuilds `#mobile-navbar-scroll` and `#mobile-navsheet-list` from scratch (`innerHTML =` a mapped template) every time the order/hidden state changes, then calls `applyIcons()` again (idempotent — safe to re-run on already-filled icons). This REQUIRED converting tab click handling from one `addEventListener` per `.tab` element (attached once at load) to a single delegated `document.addEventListener('click', ...)` checking `e.target.closest('.tab')` — freshly-rendered elements need to work immediately with no separate rebind step, which only event delegation provides. Desktop's `#tabs` stays fully static and is completely unaffected by any of this — the delegated handler works identically for static and dynamically-rendered `.tab` elements, it doesn't care which.
**Settings is permanently pinned, outside the whole customization system** — not in `MOBILE_NAV_ITEMS`, not reorderable, not hideable, always the sheet's last row. Necessary: it's the only way back into the settings that control everything else in this list, so it can't be one of the things that can be hidden/reordered away.
**Two safety guards, same shape as similar ones elsewhere in this file**: (1) hiding a page can't drop `visibleMobileNavOrder()` below `MOBILE_NAV_TOOLBAR_SLOTS` (5) — not just "not zero"; per explicit follow-up request the toolbar should never end up with fewer than 5 tabs (plus the always-pinned Settings row) just from hiding pages, so the checkbox snaps back checked once exactly 5 remain visible, same pattern as Overview Boxes to Show's "can't deselect the last one" guard just with a higher floor; (2) hiding the CURRENTLY ACTIVE page auto-navigates to whatever's now first in the visible list (`document.querySelector('.tab[data-page="${fallback}"]').click()`) rather than stranding the user on a page nothing in nav points back to. Verified directly: attempting to hide all 12 pages one by one stops accepting hides at exactly 5 remaining, every further attempt snaps back to checked.
**Reordering is up/down arrow buttons on each Settings row, not drag-and-drop** — a deliberate choice, not a limitation worked around: touch drag-and-drop needs a real custom pointer-event implementation (no native HTML5 DnD support on touch) with meaningfully more risk of real bugs and no reliable way to verify actual touch-drag behavior in this environment, versus simple, robust, fully-verifiable button taps that swap two array entries. If real drag reordering is wanted later, that's a deliberate feature to build carefully, not a quick upgrade.
**The toolbar can never drop below `MOBILE_NAV_TOOLBAR_SLOTS` (5) visible pages** — per explicit follow-up request, the Settings change-handler's guard threshold was raised from "not zero" to "not below 5"; every hide attempt past that point snaps the checkbox back to checked. Verified directly: hiding all 12 pages one by one stops accepting hides at exactly 5 remaining.
**The divider/expand button are ALWAYS shown now, never hidden — a real lockout bug, found and reverted**: an earlier version hid the whole expand mechanism whenever the CUSTOM sheet list (`sheetKeys`, i.e. visible pages beyond the first 5) was empty, reasoning "a menu that could only ever show the pinned Settings row alone isn't worth offering." That reasoning was wrong in practice: reaching exactly 5 visible pages (now the guaranteed floor, see above) is specifically the state where Settings being reachable matters MOST — it's the only way back in to un-hide anything. Settings guarantees the sheet is never truly empty, so there is no state where hiding the expand mechanism is actually correct — don't reintroduce that "hide when empty" logic.
**A stale saved order self-heals** — `loadMobileNavState()` filters out any saved key no longer in `MOBILE_NAV_ITEMS` and appends any canonical key missing from a saved order (e.g. if a new page is added to this list later), so a future page addition doesn't just silently fail to appear for a returning visitor with an already-customized order.

### Tapping anywhere on the mobile toolbar closes an open nav sheet, and doubles as the toolbar's haptic tap
One delegated listener on `#mobile-navbar` (not per-tab) calls `vibrate(8)` unconditionally, then closes the sheet unless the tap landed on `#mobile-navbar-expand` (which already owns its own open/close toggle and would otherwise get closed immediately after opening). This covers "tap a primary tab while the sheet is open" (switches page AND dismisses the sheet — previously the sheet only closed when a *sheet* tab was clicked, not a bar tab) and gives every icon in the bar a light tap buzz for free, without wiring haptics onto each tab individually.

### Haptics are a single global on/off, not just present on charts
`vibrate(ms)` (defined near `IS_MOBILE`, early in `<script>`) is now the ONE place that calls `navigator.vibrate` — both `attachHaptics()` (chart scrubbing) and the mobile toolbar listener above route through it, gated by the shared `hapticsEnabled` flag (persisted to `localStorage['gp_haptics_enabled']`, default on). Before this, chart haptics called `navigator.vibrate` directly and there was no way to turn it off; a new `navigator.vibrate`/haptic call added anywhere else on the page should go through `vibrate()` too, not call the API directly, or it'll silently bypass the Settings toggle.

### Settings page (`#page-settings`) — a 13th tab, sheet-only (no primary-bar slot), preferences persisted to `localStorage`
Three controls, each independent, each with its own key: **Default Time Range** (`gp_default_range`) — the row is a real `[data-range-slot]` container so `renderRangeToggles()`/the shared range-button click delegate already keep it in sync with every other range row for free; a *second*, scoped listener on just this row is what additionally persists the choice as next-load's `DEFAULT_RANGE_KEY` (every other range row on the page deliberately does NOT persist — clicking those only changes what's on screen right now). **Overview Boxes Per Row** (`gp_overview_cols`, 2–4, default 2) — an `<input type=range>` that sets a `--ov-cols` CSS custom property on `:root`, which `#page-overview .stats-row-inner`'s mobile grid rule reads via `repeat(var(--ov-cols,2), 1fr)`. **Haptics** (`gp_haptics_enabled`) — see the rule above. **Test Mode** is NOT a separate concept — the Settings toggle just calls the same `setTestMode()` the header `#test-mode-btn` does, and `setTestMode()` was extended to sync both controls' visual state (whichever one you didn't click) since they're two views of one `testModeActive` flag. `#test-mode-btn` itself is now `display:none` on mobile (same reasoning `#download-agent-btn` already had: tight header space, Settings is the one place for "how this behaves" prefs on mobile) but still fully live on desktop, where there's no Settings-page equivalent visible in the primary flow (Settings is still reachable from desktop's `#tabs` too, just not the only way to reach Test Mode there).
Styled as OneUI-8.5-*inspired* toggles/sliders (`.oui-toggle`, `.oui-slider`) built entirely from this file's own tokens (`--accent`/--accent2`/`--panel`/`--border`/`--ease`) rather than copying Samsung's literal palette — the ask was "fitted to the website color scheme". The toggle is a real `<input type="checkbox">` under custom-styled `~`-sibling markup (not a clickable `<div>`), so it stays keyboard/screen-reader accessible without extra ARIA work. The slider's two-tone filled track (`--fill`, a percentage) has to be computed and set from JS on every `input` event — a plain range input can't do a two-tone track from CSS custom properties alone without that.
**Haptics and Overview Boxes Per Row are mobile-only cards** (`.settings-mobile-only`, `display:none` by default, turned back to `display:flex` inside the existing `max-width:860px` block) — neither means anything on desktop (Overview always shows the full chart grid there, never the stat-tile grid; haptics is a device capability that's a no-op on desktop hardware anyway). Default Time Range and Test Mode stay visible on both, since both are meaningful desktop settings too.
**`.oui-slider`'s track is 14px thick with an 18px thumb**, not the original 6px/22px pairing — OneUI 8.5's real sliders read as a bold filled bar with a comparatively small thumb resting on top, the original pairing read more like a stock Material Design slider. `.oui-slider-row` has `9px` of horizontal padding (half the thumb's own width) — native range inputs let the thumb's *center*, not its edge, travel the full 0%–100% of the track, so without this padding the thumb visibly pokes half its own width past the track's ends at the min/max positions, reading as spilling over the settings card's border.
**Default Time Range does NOT trigger a haptic tap** — every other Settings control does (`vibrate()` on toggle/slider change), but this one only persists the choice to `localStorage`, deliberately with no `vibrate()` call, per explicit request.
**Overview Boxes Per Row is 1–3, not 2–4** (default still 2) — `overviewCols` is clamped to `[1,3]` at load (`Math.min(3, Math.max(1, ...))`) so a value saved by an earlier version of this setting (when the range was 2–4) doesn't leave a returning visitor stuck at an now out-of-range 4. The mobile Overview's stat-tile text sizes (`#page-overview .stat-label`/`.stat-value`/`.stat-unit`, in the `max-width:860px` block) are no longer fixed pixel values — each is a `calc()` scaled by `2 / var(--ov-cols)` (so the original hand-tuned numbers, which assumed 2 columns, reproduce exactly at `--ov-cols:2`, scale up for the wider 1-column tile, and scale down for the narrower 3-column one), still `clamp()`-bounded so text can't grow large enough to overflow a narrow tile or shrink small enough to be unreadable on a wide one. `applyOverviewCols(n)` also toggles a `:root.ov-cols-3` class (a plain CSS var can't be branched on with a selector) — `:root.ov-cols-3 #page-overview .stat-unit { margin-top:-3px; }` nudges the unit text up slightly at 3 columns, where the tile is short enough that the normal flex-column gap between value and unit reads as too much air.
**Overview Boxes to Show** (`gp_overview_mobile_visibility`, `.settings-mobile-only`) — a 13-item checkbox list (`OV_MOBILE_ITEMS`) letting the user hide individual mobile-Overview stat tiles, same pattern as the desktop "Visible Charts" customize panel (`OV_VIS_KEY`/`ovVisible`/`renderCustomizePanel()` above) but targeting `.stat-card[data-ov-key]` instead of `.mini-chart-card[data-metric]` — every stat-card in `#page-overview`'s markup carries a `data-ov-key` matching its `ov-<key>` id suffix for this to select by. Hiding is `.ov-mobile-hidden { display:none; }`, itself scoped INSIDE the `max-width:860px` block, so toggling a box off here can never affect desktop's Overview (which always shows the full stat bar regardless of this preference).
**Settings card descriptions were removed** — an earlier pass added a `.settings-card-desc` line under every title explaining what each control does; trimmed per explicit request ("remove unnecessary description text"). Titles/tick labels/the slider's own value badge are considered self-explanatory enough on their own now — don't reflexively add an explanatory paragraph under a new Settings control without a specific reason it's actually needed.

### X-axis tick count is mobile-aware too (`IS_MOBILE`, shared with `MAX_POINTS`)
Chart labels are hour:minute only (`fmtTimeNoSeconds`, no seconds) — fine on desktop's ~30s window, but mobile's ~12s window (`MAX_POINTS=60`) is short enough that most/all ticks land in the same minute and print the identical label repeated. `maxTicksLimit` drops to 2 on mobile (was 4/8) everywhere it's set (mini charts, dedicated "big" charts, Multi) — all three sites read the same `IS_MOBILE` constant, keep them in sync if this changes again.

### Lap timing is a geofence crossing, Efficiency is Wh accounting — both are pure client-side derived state, nothing new stored server-side
Neither feature added a database column, a `telemetry_packet_t` field, or a `server.js` route — both are computed entirely in `index.html` from fields that already exist (`latitude`/`longitude`/`flags`, `batt_volt`/`current_a`/`speed_mph`). This matters because it means **laps are never stored** — see the dedicated rule below on how a past session's laps are recomputed on demand instead, not fetched.
- **The geofence can also be set by tapping the map directly** — a floating "Set Start/Finish" button (`.raceline-map-overlay-btn`, a sibling of `#gps-map`, not a descendant — deliberately outside Leaflet's own `isolation:isolate` stacking context, see that rule elsewhere, so it never has to compete with Leaflet's internal pane z-indexes) toggles `geofencePickMode`; while on, the NEXT tap anywhere on the Raceline map (`gpsMap.on('click', ...)`) calls `setGeofenceFromMapClick(lat, lon)` and pick mode turns itself back off. This is a THIRD way to set the same geofence, alongside typing lat/lon into Settings directly and "Use Current GPS Position" — all three ultimately funnel through the same `saveGeofenceFromInputs()` (the map-tap path fills the Settings `<input>` values via `document.getElementById(...)`, exactly like "Use Current GPS Position" already did, then calls it) rather than three separate save/persist implementations. A normal map tap with pick mode OFF does nothing to the geofence — Leaflet's own `click` event only fires for the base map/tiles, never for clicks on its own zoom/attribution controls, so those can't accidentally trigger this either.
- **`updateGeofenceMapLayer()` draws an `L.circle` (amber, dashed) at the configured geofence on the Raceline map** — redrawn after every change (any of the three ways above, or Clear) and once on initial load if a geofence was already saved from a previous visit. Plain hardcoded hex color (`#f59e0b`), not a `var(--x)` CSS custom property — matches how `gpsPolyline`/`gpsMarker` are already styled elsewhere in this same section, since Leaflet's own styling options don't reliably resolve CSS custom properties the way a real DOM element's `style`/class would.
- **The geofence (Settings → "Lap Timing — Start/Finish Geofence")** is a lat/lon/radius-meters trio in `localStorage` (`gp_geofence_lat`/`gp_geofence_lon`/`gp_geofence_radius_m`, read once into `geofenceLat`/`geofenceLon`/`geofenceRadiusM`). "Use Current GPS Position" fills lat/lon from `lastGpsFixForDistance` (the most recent VALID live GPS fix, kept current by `onLiveGpsFix()` on every packet regardless of which tab is active — same reasoning the raceline trail itself already uses) rather than requiring the user to be on the Raceline tab or type coordinates by hand. An incomplete pair (one field filled, one blank) is treated as "not configured" (`geofenceConfigured()` requires both), not a half-saved geofence using a stale value for the other half.
- **A lap is recorded on OUTSIDE→INSIDE crossings only** (`checkLapCrossing()`, edge-triggered via a `geofenceInside` boolean), not "currently inside" — the car sitting inside a wide zone for several packets in a row must not register a lap per packet. `LAP_MIN_MS` (5000ms) additionally debounces the crossing itself — a GPS fix wobbling right on the boundary can't register a string of near-zero-length laps; a real lap at this vehicle's speeds is always many seconds at minimum.
- **Distance is GPS-integrated (haversine) between consecutive valid fixes**, capped by `GPS_JUMP_CAP_M` (50m) — a real vehicle can't jump this far between ~200ms-apart fixes (50m/200ms ≈ 560mph), so a jump past that cap is treated as a GPS glitch and skipped rather than added as real distance; a small minimum (0.5m) similarly keeps GPS jitter while stationary from slowly accumulating fake distance.
- **Consumed Wh integrates `batt_volt * current_a` (pack power), not `motor_volt`** — the point is to reflect energy actually drawn FROM THE BATTERY, and `motor_volt` is downstream of the ESC/controller, not the pack itself. Integrated over real elapsed time (`dtHours` from consecutive `received_at` timestamps) in `pushHistory()`, the same function that already tracks `lastPacketTime`/`activeRunStart` for the live charts — piggybacking on it rather than a second polling path.
- **Live accumulators (`liveConsumedWh`/`liveDistanceMi`/`liveLaps`/current-lap state) reset exactly when the live chart buffers already do** — the same stale-gap wipe in `pushHistory()` (`CHART_GAP_STALE_MS`, 5s) that discards a disconnected old chart run also calls `resetLapTracking()`, since a gap that long means whatever lap/trip was in progress is over too. There's also a manual "Reset Laps"/"Reset Trip" button on each tab (both just call `resetLapTracking()` — one shared reset for both features, since they share accumulators).
- **`renderLiveLaps()`/`renderEfficiencyLive()` also run on a plain 1-second `setInterval`**, not just on new packets — the current lap's running timer and `avgPowerW`'s elapsed-hours denominator are ELAPSED-TIME-based and should keep visibly ticking even between live packets, the same reasoning the header's own `server-time` clock already uses.
- **Placement of this whole section in the script is deliberate, not arbitrary** — it sits AFTER `activeRunStart`/`historyTimes`/`HISTORY_HARD_CAP_MS` are declared (in the STATE/HISTORY block) because its own top-level code (the two `renderLiveLaps()`/`renderEfficiencyLive()` calls at the bottom) reads `activeRunStart` immediately, not just from inside a function called later — and a `let` read before its own declaration line has executed throws a real `ReferenceError` (the exact same trap already documented elsewhere in this file for the `history`-variable-shadowing bug). The function DECLARATIONS in the section would be safely hoisted regardless of where they sit; the plain top-level statements would not.

### A past session's laps are recomputed from its stored points every time it's opened — never stored, and never using the geofence that was active when the drive happened
`analyzeSessionPoints(rows)` (near `viewSession()`) is a batch version of the exact same crossing/distance/Wh logic the live path uses (`LAP_MIN_MS`, `GPS_JUMP_CAP_M`, `gpsDistanceM`, `geofenceConfigured()` — all shared, not duplicated with different constants) run once over a session's full `GET /api/sessions/:id/points` response. Selecting "Lap N" from the `#session-lap-select` dropdown (added next to the session charts) re-renders `renderSessionCharts()` against `rows.slice(lap.startIdx, lap.endIdx+1)` instead of the full array, and updates a one-line summary (duration/distance/top+avg speed/Wh/Wh-per-mile) for whatever's currently selected — "All" shows the whole-session totals `analyzeSessionPoints()` already computed in the same pass. **This means the geofence used to slice a session's laps is whatever is configured RIGHT NOW, in Settings, not whatever (if anything) was set when that session was actually recorded** — a deliberate simplification (laps were never going to be persisted server-side, see the rule above) worth knowing if a session's lap count looks wrong after moving the geofence: reopening it re-slices by the new zone. A session recorded before any geofence was ever set just shows zero laps (`lapsRow.style.display='none'`) but still gets full trip-level totals, since those don't need a geofence at all.

### The Efficiency tab's Power chart is a derived, hand-maintained history array — not a `METRICS` entry, following the `esc_mode`/`esc_state` precedent
`historyPowerW` (parallel to `historyTimes`, pushed/gap-wiped/hard-cap-evicted at the exact same points in `pushHistory()` as `history[k]`) and `powerWChart` (`safeChart('chart-power_w', ...)`, no `metricKey` passed since there's no `METRICS[...].range` to seed the y-axis from) exist entirely OUTSIDE the `METRICS` registry, because `power_w` (`batt_volt * current_a`) isn't a raw packet field — it's computed. Putting it in `METRICS` would be wrong for the same reason `esc_mode`/`esc_state` aren't in there either (see that rule above): `METRICS` assumes every entry is a real field with a CSV column and a value straight off the packet. `applyHistory()` updates it with one explicit extra line rather than folding it into the `Object.keys(bigCharts).forEach(...)` loop, and the tab-switch resize handler calls `forceChartResize(powerWChart)` alongside the METRICS-driven charts by hand too — **if a future derived (non-`METRICS`) chartable value is added, this is the pattern to copy**, not adding a fake entry to `METRICS` just to get the existing plumbing "for free."

### `.data-table`/`.settings-input` are this file's first GENERALIZED (not just ID-scoped) table and text-input styles
Before this pass, the sessions table's look (`#sessions-table`/`#sessions-table-wrap`) and every text/number field's look were both one-off, ID- or bespoke-class-scoped rules (`.onboarding-password-input` was the only styled text input in the whole file, and it's dot-masked/centered specifically for a password stand-in — not reusable as-is). The Laps table (Raceline tab + per-session lap view) and the geofence lat/lon/radius + battery capacity fields needed the same look without a second hand-copied CSS block, so both got pulled out into real classes (`#sessions-table`/`#sessions-table-wrap` keep their IDs too, for the existing JS/`COPYABLE_SELECTOR` references, they just ALSO carry the class now). **If a future table or number/text field is added anywhere else in this file, reach for `.data-table`(`-wrap`)/`.settings-input` first** rather than another bespoke block — this is meant to be the shared baseline now, not a one-off for these two features.

### The new mobile nav item (`efficiency`) sits between `raceline` and `link` in `MOBILE_NAV_ITEMS` — after `MOBILE_NAV_TOOLBAR_SLOTS` (5), so it still doesn't touch the default toolbar
First shipped appended at the very END of the array (after `raw`), specifically so inserting it wouldn't change which 5 keys land in the toolbar BY DEFAULT for a brand-new visitor (the first `MOBILE_NAV_TOOLBAR_SLOTS` entries of `MOBILE_NAV_ITEMS`'s own order — see the rule above on why that first-5 order is deliberate and "shouldn't change without deliberately changing the shipped default"). Moved to its current position (between `raceline` and `link`, matching the desktop `#tabs` order — see below) per an explicit follow-up request — this is still safe for the SAME reason: position 9 of 13 is well past the 5-slot toolbar cutoff, so a new visitor's default toolbar is still exactly `overview`/`speed`/`power`/`rpm`/`esc`, unchanged; Efficiency just lands earlier within the "More" sheet's own order (right after Raceline) instead of at its very end. **Only the first 5 positions in this array are toolbar-sensitive** — anywhere at position 6+ is safe to reorder for pure "where does this read naturally in the sheet" reasons without touching the shipped toolbar default. A RETURNING visitor with an already-customized saved order is unaffected either way — `loadMobileNavState()`'s self-healing logic only appends a canonical key MISSING from a saved order (to the end of it); reordering a key that's already present in every existing saved order doesn't trigger that path at all, so an existing customized layout keeps its own Efficiency position exactly where the user last left it.
**Desktop's `#tabs` bar got the same move** (`data-page="efficiency"` moved from between Power/Temp to between Raceline/Link) for consistency between the two nav orders — desktop has no toolbar-slot concept to worry about (all tabs always show), so this was a pure "which order reads better" call with no default-disruption risk either way.

### Efficiency stats now also appear as Overview stat cards — same derived values, written to a SECOND set of element ids by `renderEfficiencyLive()`
Three new `.stat-card[data-ov-key]` cards (`eff-consumed`/`eff-remaining`/`eff-wh-per-mile`, ids `ov-eff-consumed-wh`/`ov-eff-remaining-wh`/`ov-eff-wh-per-mile`) sit in `#overview-stats-row` alongside every other Overview tile. Desktop always shows the full stats row (no per-card toggle there — same as every other Overview tile), so adding these three to the markup was enough to put them on desktop by default with no other change needed. `renderEfficiencyLive()` writes to these THREE ids in addition to the Efficiency tab's own `eff-consumed-wh`/`eff-remaining-wh`/`eff-wh-per-mile` — same "write to more than one id per value" pattern `applyState()` already uses for every real `METRICS` field (`ov-`+k and `big-`+k both get the value) — via `setValueText` (not `setText`, unlike the Efficiency tab's own elements) specifically so these three ALSO get the same brief "value changed" flash every other Overview stat card gets on update.
**On mobile, these three replaced Motor Volt/Motor RPM/Packet # in the DEFAULT-visible set, not just added alongside them** — see the "Overview Boxes to Show" rule above for the exact default-set change and why it's a swap, not an addition. `OV_MOBILE_ITEMS` gained the same three keys (for the Settings customize list), all fully toggleable — this only changed what starts checked, not what's possible to show.

### `.data-table-wrap`/`#sessions-table-wrap` needed `flex-shrink:0` — a real bug, not implied by the class already existing
Found via a real user report ("the bottom of the raceline page on mobile is bugged") — the Lap Timing table on the Raceline tab was rendering at ~2px tall, effectively invisible, despite having real rows in its `<tbody>`. Root cause: `.data-table-wrap` sets `overflow:auto hidden` (needed for the horizontal-scroll behavior itself, see its own rule elsewhere), and per the CSS Flexbox spec, a flex item's AUTOMATIC minimum size — what normally stops `flex-shrink` from crushing an item below its own content size — is 0 (not content size) for any element whose `overflow` isn't `visible`. Inside `#page-raceline`'s flex COLUMN, once `.raceline-top-row` above it claims its `flex-shrink:0`-protected 50vh on a short mobile viewport, every UNPROTECTED sibling gets squeezed to fit the remainder — and because `.data-table-wrap`'s automatic minimum is 0 (not "as small as its actual rows"), it was the one that lost that fight and got crushed nearly flat, while `.big-row` (no `overflow` set, so ITS automatic minimum stays its real content size) survived untouched. Fixed with an explicit `flex-shrink:0` on the shared `#sessions-table-wrap, .data-table-wrap` rule — the page itself already scrolls (`.page{overflow-y:auto}`) to show whatever doesn't fit, so nothing needs to shrink to make room; a table should either show at full size or make the page scroll, never silently collapse. **This same trap could in principle affect `#sessions-table-wrap` too** (same overflow value, same flex-column-with-more-content-than-fits situation on the Sessions tab) — it hadn't been reported as visibly broken there, but the fix was applied to the SHARED rule rather than scoped to just the Raceline case, so both are protected now. **If a future scrollable, `overflow`-bearing container is ever placed inside a flex column with other content competing for space, check whether it needs its own explicit `flex-shrink:0` or `min-height`** — an element that "has real content but renders as a sliver" in a flex column is this exact spec behavior, not a sizing calculation bug to chase elsewhere.

### The Efficiency tab's Power chart is deliberately taller on mobile — `.chart-card-tall`, `!important` required
Per explicit request ("make the efficiency graph on mobile larger"). The generic mobile rule (`.chart-card { min-height:200px; }`) couldn't just be bumped globally without affecting Speed/Temp/Multi's charts too, so a dedicated `.chart-card-tall` class (currently only on the Power chart's card) sets `min-height:340px !important` instead — `!important` is required, not a style choice, because this chart-card's own inline style sets `min-height:0` (the same deliberate flexbox-overflow fix `.chart-canvas-wrap` uses elsewhere in this file), and a plain external rule — regardless of selector specificity — can never beat a non-`!important` inline style; only `!important` in the stylesheet can. Same technique this file already uses for `.chart-row`'s mobile `grid-template-columns` override, just applied to a new class instead of an existing one.

---

## Current State (V3.5 — Lap Timing, Efficiency tab, per-lap session view)

- **New "Lap Timing" section on the Raceline tab** — geofence-based lap recording (current-lap running timer, best-lap card, a table of completed laps with delta-to-best/distance/top speed/efficiency), plus a "Reset Laps" button. Geofence itself (lat/lon/radius-meters) can be set three ways now: typing lat/lon into the Settings card directly, "Use Current GPS Position" (also in Settings), or a floating "Set Start/Finish" button ON the Raceline map itself — tap it, then tap the map (the car's current position or any other point) to place it there. See the dedicated rule above for the crossing-detection/debounce/distance-cap details and how the three set-geofence paths share one save function.
- **New "Efficiency" tab** (desktop `#tabs` and mobile `MOBILE_NAV_ITEMS`, both between Raceline and Link — moved there from an initial Power/Temp desktop position and end-of-list mobile position, per follow-up request; still lands in the "More" sheet by default on mobile, not the primary toolbar, see rule above) — Consumed/Remaining Wh, Wh-per-mile, Distance, Estimated Range, Average Power, and a live Power (W) chart, plus a "Reset Trip" button.
- **`batteryCapacityWh` is a fixed `const` (840), not a Settings field** — briefly shipped as a user-editable Wh input (with a "--" fallback for Remaining Wh/Estimated Range until filled in), removed per explicit follow-up once the real number was known: the car's actual pack is two Interstate DCM0035 (12V, 35Ah each) in series — series wiring adds voltage not amp-hours, so that's 24V at 35Ah, `24 * 35 = 840`Wh, not a per-user preference to expose in the UI. If the car's actual battery pack ever changes, update this one constant (declared right next to the geofence state, in the "EFFICIENCY / LAP TIMING" section) — there's deliberately no Settings UI for it anymore, don't re-add one without being asked; the removed Settings card/input/localStorage-key/"set your capacity" hint text were all torn out together, not just hidden.
- **The current-lap running clock (`updateCurrentLapTimer()`) ticks on its own 10ms `setInterval`**, separate from the once-a-second `renderLiveLaps()`/`renderEfficiencyLive()` tick — see rule above for why it's split out (avoids rebuilding the laps table's innerHTML 10-100x/sec for a value that's only actually changed by a real crossing).
- **Multi/Sessions/Raw are hidden by DEFAULT on mobile now** (`mobileNavHidden` starts as `['raw','sessions','multi']` instead of `[]`) — per explicit request. Still fully present in `mobileNavOrder`/reachable and re-showable via Settings → Mobile Toolbar & Pages (one tap), not removed from the code the way an earlier pass once fully removed them from mobile nav entirely — see rule above for the distinction. A RETURNING visitor's own saved `gp_mobile_nav_hidden` (if any) still wins over this default, same as always.
- **`batteryCapacityWh` is now a fixed `const` (840), not a Settings field** — see its own rule above for the exact math (2× Interstate DCM0035, 12V/35Ah, in series). The Settings card/input/localStorage key for it were fully removed, not just hidden.
- **Three Efficiency stats (Consumed/Remaining/Wh-per-mile) now also show as Overview stat cards** — on desktop, added alongside every existing tile; on mobile, they REPLACED Motor Volt/Motor RPM/Packet # in the default-visible set specifically (all three still fully present/re-enableable, just default-OFF now) — see the dedicated rule above for exactly how `renderEfficiencyLive()` double-writes these values and why the swap, not a pure addition, was the actual request.
- **A real bug fixed, found via user report**: the Raceline tab's Lap Timing table was rendering ~2px tall (effectively invisible) on mobile — a genuine CSS Flexbox spec behavior (an `overflow`-bearing flex-column child's automatic minimum size is 0, not its content size), not a sizing miscalculation. Fixed with `flex-shrink:0` on the shared `.data-table-wrap`/`#sessions-table-wrap` rule — see the dedicated rule above for the full mechanism, worth remembering for any future scrollable container placed inside a flex column with competing content.
- **The Efficiency tab's Power chart is taller on mobile** (`.chart-card-tall`, `min-height:340px !important`, up from the generic 200px every other single-chart dedicated page uses) — per explicit request, scoped to just this one chart via a dedicated class rather than a global bump. See its own rule above for why `!important` was actually required here, not just convenient.
- **Both features share one set of live accumulators** (`liveConsumedWh`/`liveDistanceMi`/`liveLaps`/current-lap state) that reset together, exactly when the live chart buffers already reset on a stale data gap — see rule above. Nothing here is stored server-side; no new DB column, packet field, or route was added.
- **A past session's Sessions-tab detail view now has a lap selector** when the session has recomputable laps under the CURRENT geofence setting — picking "Lap N" re-renders that session's charts scoped to just that lap's time range and shows a duration/distance/speed/Wh summary line for it; "All" shows the whole session. Laps are recomputed fresh from the session's stored points every time it's opened, not stored — see rule above for what that does and doesn't mean if the geofence has moved since the session was recorded.
- **`.data-table`/`.settings-input` are new, generalized CSS classes** (table look and text/number-input look, respectively) — the Laps table and the geofence/battery-capacity Settings fields use them, and `#sessions-table`/`#sessions-table-wrap` were extended to also carry the table class (keeping their original IDs too). See rule above — reach for these first for any future table/input rather than another one-off block.
- **The Efficiency tab's Power (W) chart is a derived, hand-maintained chart** (`historyPowerW`/`powerWChart`), deliberately NOT added to the `METRICS` registry since it isn't a raw packet field — see rule above for the `esc_mode`/`esc_state`-precedent reasoning.
- **Bug-tested via Test Mode's synthetic circular GPS path** (already existed for UI testing generally) — a geofence placed on that circle correctly produces multi-minute laps with sane distance/duration once fed a realistically-advancing clock (a synchronous tight-loop test using real `Date.now()` per tick does NOT work, since `LAP_MIN_MS`'s debounce is real-wall-clock-based and a synchronous loop barely advances real time — this is a testing-methodology gotcha, not a bug, worth remembering if lap detection ever looks like it "isn't working" under a similar fast-forward test). Every one of the 14 tabs (including the two new ones) switches cleanly with no console errors and every chart resizes to real (non-zero) dimensions once its page is actually active; the only console errors seen anywhere in this pass were the expected `/api/sessions` 503s from a local dev environment with no `DATABASE_URL` configured, not anything introduced by this work.

## Current State (V3.4 — Settings page, decluttered nav sheet, global haptics toggle)

- **New Settings page** (`#page-settings`, reached only via the mobile nav sheet's Settings tab — see rule above) — Default Time Range, Overview Boxes Per Row (2–4, mobile), Haptics on/off, and Test Mode, all persisted to `localStorage`. Styled with new OneUI-8.5-inspired `.oui-toggle`/`.oui-slider` components built from this file's own color tokens.
- **Mobile nav sheet no longer duplicates the 5 primary bottom-bar tabs** — only lists the other 7 pages + Settings. Tapping anywhere on the toolbar (not just a sheet tab) now closes an open sheet, and gives a light haptic tap.
- **Haptics have a single global on/off** (`vibrate()` helper, `gp_haptics_enabled` in `localStorage`) — both chart-scrub haptics and the new toolbar-tap haptics route through it; previously only chart haptics existed and there was no way to disable it.
- **`#test-mode-btn` moved off the mobile header into the Settings page** as a toggle (still live on desktop's header) — see rule above for how the two stay in sync.
- **Haptics and Overview Boxes Per Row settings are mobile-only** (`.settings-mobile-only`) — hidden on desktop, where neither setting means anything. Default Time Range and Test Mode stay visible on both.
- **Overview Boxes Per Row is 1–3 (default 2), not 2–4** — the mobile Overview stat-tile text sizes now scale with `--ov-cols` instead of being fixed for 2 columns.
- **`.oui-slider` is thicker (14px track / 18px thumb)** for a closer OneUI-8.5 look, with padding added to keep the thumb from visually poking past the settings card's border at the slider's min/max positions.
- **Default Time Range no longer triggers a haptic tap** when changed from Settings — every other Settings control still does.
- **`-webkit-touch-callout:none` added alongside `user-select:none`** — closes a real gap where iOS Safari's long-press "Copy" callout menu could still appear on non-data UI text even though it wasn't visibly selectable.
- **Raceline map auto-zooms to fit the recorded trail** instead of following at a fixed zoom — see rule above.
- **Raceline map no longer paints over the mobile toolbar** (`#gps-map { isolation:isolate }`) — a real Leaflet/stacking-context bug, see rule above.
- **New "Overview Boxes to Show" settings panel** — per-tile checkboxes for the mobile Overview grid, `.settings-mobile-only`. See rule above.
- **Android Chrome's long-press "Smart Text Selection" popup is blocked on non-data text** via a document-level `contextmenu` handler — CSS `user-select`/`-webkit-touch-callout` alone weren't enough. See rule above.
- **Settings card descriptions removed** — trimmed per explicit request.
- **Overview Boxes to Show can't be emptied to zero**, and its default set is a specific 8 boxes (Speed/Battery/Motor Volt/Current/Temp/Motor RPM/Wheel RPM/Packet #), not all 13 — see rule above.
- **New `GET /api/version` endpoint** (`server.js`) + client-side update-available banner (`#update-banner`) — see rule above.
- **`manifest.json`'s `orientation` is now `"portrait-primary"`** (was `"any"`), plus a best-effort `screen.orientation.lock()` and a CSS-only landscape-block overlay as fallbacks — see rule above.
- **Android back gesture now shows a "press back again to exit" toast** instead of silently closing the installed WebAPK — rewritten from an unreliable modal-with-a-button approach to rely on a genuine second real back press instead, see rule above (also documents a real bug caught along the way: a pre-existing `const history` chart-buffer variable shadows the global History API in this script).
- **Gradient fills now use `to right` everywhere, not `135deg`** — fixed a real visible bug on every wide-but-short and even the one previously-assumed-safe near-square gradient element — see rule above.
- **Every full-screen mobile dimming backdrop fades in AND out symmetrically** (was fade-out only) — see rule above.
- **First-visit onboarding/access gate is now 4 steps**: required `'SentinelRacing26!'` password → a platform-specific optional install step (the app on mobile, the receiver agent on desktop) → feature overview → a final Settings tour (Open Settings or skip to the dashboard) — see rule above. Animated with a gradient-button, staggered-feature-list treatment matching the rest of the site; the password field is CSS-masked `type="text"`, not `type="password"`, to avoid Chrome's save-password prompt. Still a UI speed-bump, not real access control; every API endpoint's auth is unchanged.
- **The mobile install step now waits for the real install + a fresh app launch before advancing**, resuming at the overview step (page 3) instead of restarting — see rule above.
- **The onboarding overlay's background now matches Android's system nav bar on mobile** (same gradient `#app` uses) instead of a flat color that mismatched it — a real CSS cascade-order bug caught and fixed, see rule above.
- **Mobile nav (toolbar + sheet) is now fully user-customizable from Settings** — reorderable and individually hideable per page, Multi/Sessions/Raw restored as part of this (no longer hard-removed) — see rule above. This required converting tab-click handling to event delegation; desktop unaffected.
- **`.mobile-navbar-tab-primary`'s icon-centering padding bug fixed** — inherited asymmetric padding from the shared `.tab` class, see rule above.
- **Every full-screen dimming backdrop on mobile now fades out before reaching the nav bar** (`.confirm-modal-backdrop`, `.update-banner-backdrop`, same treatment `.mobile-navsheet-backdrop` already had) — see rule above.
- **New Settings → Clear Website Data button** — wipes all local storage/cookies/service-worker/cache state and reloads (which re-shows the onboarding gate) — see rule above.
- **Update banner is now required, not dismissible** — see its own rule further up.

---

## Current State (V3.3 — mobile UI polish round 2: test mode, haptics, animated/smoother charts, symmetric nav, select-none)

- **App name on install** is now "GP Telemetry" (`manifest.json`'s `short_name`, and the `apple-mobile-web-app-title` meta tag) — was "Greenpower"/"Greenpower Telemetry". An *already-installed* PWA on a device won't pick this up (or the theme-color/status-bar-color fixes noted in SESSION_NOTES.md) until it's uninstalled and reinstalled — this is a platform manifest-caching behavior, not something fixable from this repo's code.
- **Test Mode** (`#test-mode-btn` in the header) — see rule above.
- **Mobile bottom nav bar squares are now symmetric**: `.mobile-navbar` itself is the single flex row (`justify-content:space-between`) with `.mobile-navbar-scroll` reduced to `display:contents` — previously the 5 tabs were evenly spaced only *within their own sub-box*, while the divider+expand button sat outside it with mismatched edge margins, reading as off-center.
- **IMU page's two stacked `.chart-row` sections no longer overlap on mobile** — `.big-row`/`.chart-row` are `flex:none` (was `flex:1`) at the mobile breakpoint, so each section's box sizes to its actual grid content instead of being compressed to a flex-share that overflowed into the next section.
- **Live charts animate on update, use fewer points on mobile, and render smoother lines** — see the three rules above (`LIVE_ANIMATION`, mobile `MAX_POINTS`, `borderJoinStyle`/`borderCapStyle:'round'`).
- **Chart scrubbing triggers light haptic feedback on Android** — see `attachHaptics()` rule above.
- **Multi tab's legend markers are short colored lines, not boxes** (`usePointStyle:true, pointStyle:'line'`) — less horizontal space per entry across up to 18 toggleable metrics.
- **Only telemetry values are selectable/copyable** — see rule above.
- **Mobile dedicated-value pages (Power/RPM/ESC/IMU/Link) lay out as 2-per-row landscape-rectangle tiles, with an odd one out spanning full-width** — see rule above.
- **`color-scheme:dark` declared** (meta + CSS) so Android's installed-PWA status/nav bar chrome follows the page's dark theme — see rule above; still needs an uninstall+reinstall to take effect on an already-installed copy.
- **`display` stays `"standalone"`** — a `display_override:["fullscreen","standalone"]` attempt at the nav-bar color mismatch was reverted at the user's request (fullscreen was buggy on their device); see rule above for the current state of that limitation and what a real fix would require.
- **Mobile edges (body background, `#app`'s background, `theme-color`, manifest splash colors) match Android's actual measured system-bar colors** (`#0b0c11` top / `#171719` bottom) instead of continuing to fight Chrome to match ours — see rule above. Desktop is completely untouched.
- **Live charts use a real linear time x-axis with fixed `{x,y}` points, no per-update animation** — see rule above; fixes both "line bugs out" (index-remapping wiggle from the earlier animation approach) and "Test Mode graphs don't show up" (the resize-deferral trap that approach caused).
- **Mobile nav sheet's close animation actually plays now** — see rule above; it silently never animated before (`display:none` snap).
- **`.mobile-navbar` has a small upward-biased shadow again** (`0 -2px 8px rgba(0,0,0,.3)`) for real depth, without reaching the Android nav bar below it — see rule above.
- **Scrollable+rounded elements use `overflow:hidden auto`/`auto hidden`**, not a bare single-axis `overflow-y`/`overflow-x` — fixes a real bug where the scrollbar drew outside the border-radius curve — see rule above.
- **`.chart-hide-btn` icon is properly centered** (flex, not text-align/line-height) — see rule above.
- **X-axis tick count is mobile-aware** (`IS_MOBILE`, shared with `MAX_POINTS`) — 2 ticks on mobile instead of 4/8, since the shorter mobile time window was otherwise printing the same minute-resolution label repeated.
- **Download Agent button now downloads `public/GreenpowerAgentSetup.msi`**, a real WiX-built Windows installer (was, in order: the `download-directory.github.io` redirector → `greenpower-agent.zip` → an Inno Setup `.exe`, dropped after it was found to be hard-blocked on Intune-managed devices) — behind a confirmation modal first, since it's a real installer someone's about to run, not an inert zip. Confirmed by the user actually installing it successfully. See the rule below.
- **Chart tooltips disappear immediately when a touch is released** (`attachTouchTooltipAutoHide()`) — Chart.js has no built-in touchend handling, so a touch-scrubbed tooltip used to just stay pinned forever; see rule below.
- **Live charts have a global time-range toggle** (button row above the charts on each dedicated page + inside Overview's Customize panel: 1/5/15/30 Min or All, default 5 Min) backed by a growing, time-evicted buffer instead of the old fixed-size ring — see rule below.
- **A chart's window grows from a 1-minute floor up to the selected range, and resets on a >5s stale gap** instead of showing a mostly-empty full-range window immediately or bridging dead time with a straight line — see rule below.
- **`.mobile-navbar`'s shadow is symmetric** (`0 0 6px rgba(0,0,0,.3)`), not upward-biased — see rule above.

---

## Current State (V3.2 — mobile UI polish: icons, stable chart axes, floating install button)

- **Mobile layout added**: below `860px` width, `.mobile-sidebar` (a compact floating rounded-rectangle-chip dock, tap to expand) replaces the desktop `#tabs` bar and the Overview page's graph grid (`#overview-right-wrap`) is hidden — larger stats-bar numbers only (see mobile-only `#page-overview` sizing rules). Desktop layout (≥860px) is unchanged. Same `index.html`, no separate build/route.
- **Installable as a PWA**: `public/manifest.json` + `public/sw.js` (no caching, installability only) + `public/icon.svg`/`icon-maskable.svg`, offered via a small floating `#install-fab` button + popover (not a banner) — `beforeinstallprompt` on Chrome/Android, manual instructions via UA-sniff on iOS
- **Icon system**: all static UI icons are inline SVG from the `ICONS` map, applied to `[data-icon]` spans by `applyIcons()` — no emoji left in navigation/buttons (see rule above)
- **Chart axes are pre-filled and range-seeded** (`history`/`historyTimes` start at `MAX_POINTS` with nulls/backdated timestamps, `safeChart()` seeds `suggestedMin`/`suggestedMax` per metric) so charts don't visibly rescale/lurch as the first real data points arrive
- **Tooltips work on hover (desktop) and tap (mobile)** via `interaction:{mode:'index', intersect:false}` in `BASE_OPTS` (and mirrored in the multi-chart/session-chart configs) — needed because point radius is 0, so the Chart.js default `intersect:true` trigger could never fire
- Live path unchanged: in-memory `latest`/`lastUpdateMs`, `STALE_MS = 2000` (tuned for the 5 Hz cadence — ~10 missed packets before flagging offline)
- **Persistent path added**: optional Postgres (`DATABASE_URL`, a Railway plugin — not configured by default, degrades gracefully without it) storing every packet as JSONB, auto-grouped into sessions on a 60s gap (`SESSION_GAP_MS`)
- New endpoints: `GET /api/sessions`, `GET /api/sessions/:id/points`, `GET /api/sessions/:id/export.csv` — all public reads, same reasoning as `/api/latest` (nothing sensitive in a telemetry reading)
- **All chart x-axes now show real wall-clock time** — fixed a real bug where live charts plotted against a meaningless incrementing counter and mini-charts hid the x-axis entirely; `/api/latest` responses now carry a server-stamped `received_at` for this (see rules above)
- New **Sessions tab**: lists past sessions with duration/packet count, "VIEW" builds a one-shot set of charts from that session's full history (not live-polled), "CSV" downloads the full session via a direct link
- `CSV_COLUMNS` in `server.js` is a fixed, hand-maintained export column list — must be kept in sync with `METRICS` in `index.html` whenever a packet field is added (see rule above)
- Full tabbed dashboard (Overview, Speed, Power, Temp, RPM, ESC, IMU, Raceline, Link, Multi, Sessions, Raw) — adapted from an earlier project's dashboard design, rewired for this project's actual packet fields
- Live chart history (Overview/dedicated tabs/Multi) is still client-side-only, capped at `MAX_POINTS = 150` (~30s at 5 Hz), separate from the new persistent Sessions path — see rules above
- `TELEMETRY_API_KEY` — must be set in Railway's environment variables for production; falls back to a random per-boot key otherwise (dev-only)

---

## Self-Maintenance Requirement

After every significant change to this project, update this file's **Current State** section.

"Significant change" means: API contract change (`/api/telemetry` payload shape, auth scheme), new persistent storage added, or a new endpoint. Styling-only edits to `public/index.html` do not require a doc update.
