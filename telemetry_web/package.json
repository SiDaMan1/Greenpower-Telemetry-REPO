{
  "name": "greenpower-telemetry-web",
  "version": "1.0.0",
  "description": "Always-on dashboard for Greenpower vehicle telemetry. Shows live data when receiver_agent is forwarding, otherwise shows offline.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
